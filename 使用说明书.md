# 策略回测与策略研究工作台使用说明书

版本：1.0  
整理日期：2026-08-23  
适用工程：`tradingview-historical-backtest`

## 1. 文档目的

本文说明如何在当前工程中：

1. 准备并选择历史行情数据。
2. 使用单策略正式回测。
3. 使用“策略研究”工作台一键运行全部 108 课正式策略。
4. 正确理解资金、手续费、滑点、保证金和统一风控参数。
5. 查看任务进度、基础比较结果和磁盘留档。
6. 判断“批次完成”“策略失败”“零交易”和“进程中断”等状态。

本文只描述当前已经实现的能力。排行榜、Pareto 前沿、多策略曲线叠加和缠论行情条件归因已经由里程碑 11B–11D 提供。

> 本系统用于历史研究和软件工程验证，不构成投资建议，也不承诺任何策略盈利。

## 2. 当前可用的两种回测方式

| 方式 | 入口 | 适用场景 | 当前输出 |
|---|---|---|---|
| 单策略正式回测 | 底部“回测” | 调试一个策略、查看交易与权益明细、加载图表事件 | summary、交易、权益、订单、成交、策略事件和正式 run |
| 策略批量比较 | 底部“策略研究” | 在同一数据、资金、成本和风控口径下运行 1–13 个正式策略 | 批次进度、逐策略基础指标、comparison 留档和各成功子 run |

策略研究不是参数优化。“优化”页签用于单策略的 grid 或 seeded random 训练—验证；“策略研究”用于多个已发布策略在统一口径下横向回测。

## 3. 环境与启动

### 3.1 固定环境

- Windows 10/11 x64。
- Go 1.25.7。
- Python 3.14；工程固定使用：
  `D:\ProgramData\anaconda3\envs\pydev3.14\python.exe`。
- Node.js 22.23.2 和 npm 10.9.8。

首次安装依赖时，在仓库根目录运行：

```powershell
D:\ProgramData\anaconda3\envs\pydev3.14\python.exe -m pip install -r python/requirements-dev.lock
Push-Location web
npm ci
Pop-Location
./scripts/check-versions.ps1
./scripts/test-all.ps1
```

### 3.2 配置数据目录

检查 `config/app.yaml`：

```yaml
storage:
  data_root: ./trading-data
```

`data_root` 必须是专用数据目录，不能设置成仓库根目录、用户主目录或磁盘根目录。原始通达信 TXT 放入：

```text
<data_root>/history/
```

程序不会原地修改原始 TXT。扫描和导入后，标准化 Parquet、catalog、正式 run 与 comparison 都保存在 `data_root` 下。

### 3.3 启动系统

推荐双击根目录的 `启动程序.cmd`，或运行：

```powershell
./scripts/start-tvbt.ps1
```

开发模式可运行：

```powershell
./scripts/start-all.ps1
```

服务就绪后访问：

```text
http://127.0.0.1:5173/
```

启动脚本会启动 Python、Go 和 Vue。浏览器只访问 Go 的 `/api/v1`；不要直接访问 Python 内部端口或磁盘 Parquet。

## 4. 准备并确认数据集

1. 在右侧面板打开“数据集”。
2. 确认目标数据集已经导入且状态为就绪。
3. 单击目标数据集，使其成为当前图表数据集。
4. 在开始回测前核对：
   - 品种和合约。
   - K 线周期。
   - 数据覆盖起止时间。
   - `dataset_id`。
   - `data_revision`。

策略研究第一版固定使用当前数据集的完整覆盖区间：

- `warmup_from_bar_index = first_bar_index`
- `from_bar_index = first_bar_index`
- `to_bar_index = last_bar_index`

切换数据集后应创建新的 comparison。不同数据集不会合并到同一批次排名。

## 5. 单策略正式回测

需要调试某个策略时，先使用底部“回测”页签：

1. 选择策略。
2. 检查策略发布参数。
3. 设置资金、手续费、滑点、乘数和保证金率。
4. 检查统一风险覆盖参数。
5. 点击“开始正式回测”。
6. 等待状态变为 `completed`。
7. 在“交易”和“权益”页签查看明细。
8. 完成的 run 可作为策略对象源加载到对象树和主图。

单策略方式适合验证一个策略为何开仓、为何退出，以及交易、持仓、权益和图表事件是否一致。

## 6. 一键运行全部正式策略

### 6.1 打开工作台

在页面底部单击“策略研究”。面板会自动：

- 读取 Python 算法目录。
- 只选择 `comparison_eligible=true` 且 `research_role=formal_strategy` 的策略。
- 默认勾选当前已发布的 13 个正式策略。
- 排除 MA 示例、辅助非交易算法、独立风险过滤器和延期策略。

### 6.2 当前默认的 13 个策略

| 算法 ID | 人读名称 | 研究族 | 108 课目录映射 |
|---|---|---|---|
| `fixed_level_centre_decision_tree` | 固定级别中枢决策树 | 中枢决策 | `ALG-STR-001` |
| `downtrend_reversal_only` | 下跌趋势一买反转 | 趋势反转 | `ALG-SIG-001` |
| `trend_divergence_reversal` | 趋势背驰双向反转 | 趋势反转 | `ALG-SIG-001` |
| `consolidation_divergence_centre_reversion` | 盘整背驰中枢回归 | 中枢回归 | `ALG-SIG-002` |
| `third_buy_centre_migration_hold` | 三类点中枢迁移持有 | 三类点 | `ALG-STR-003` |
| `first_centre_B3_rotation` | 首中枢三买轮动 | 三类点 | `ALG-STR-003` |
| `second_buy_only` | 只做第二类买点 | 二类点 | `ALG-STR-002` |
| `third_buy_only` | 只做第三类买点 | 三类点 | `ALG-STR-003` |
| `centre_oscillation_spread` | 中枢震荡差价 | 中枢震荡 | `ALG-STR-004` |
| `same_level_decomposition_program` | 同级别分解机械程序 | 同级分解 | `ALG-STR-005` |
| `three_level_complete_classification` | 三层级完全分类 | 结构分类 | `ALG-STR-007` |
| `target_level_rebound_segmented_operation` | 目标级别反弹/回调分段操作 | 分段操作 | `ALG-STR-008` |
| `bottom_top_construction` | 底部/顶部构造状态机 | 底顶构造 | `ALG-STR-009` |

这里的“正式”表示已经进入可测试的交易策略通道，不表示已经证明盈利。部分策略仍是固定结构级别投影，限制见未完成事宜文档。

### 6.3 选择策略

- 点击“全选”：选择全部 13 个正式策略。
- 点击“清空”：取消全部选择。
- 展开单个策略并勾选或取消：建立任意策略子集。
- 单独选择一个策略也是合法 comparison。

### 6.4 覆盖策略参数

展开策略名称即可看到该策略的完整发布参数。未修改时使用算法目录中的发布默认值。

常见参数含义：

- `checkpoint_interval`：缠论检查点间隔；不改变策略交易语义，不建议为了追求结果而调整。
- `allow_long` / `allow_short`：是否允许多头/空头分支。
- `*_quantity`：对应分支的交易手数。
- `estimated_round_trip_cost_i64`：策略决策层估计的往返成本，使用定点整数。
- `minimum_net_range_i64` / `minimum_net_segment_i64`：扣除估计成本后要求的最小价格空间。
- `execution_available` / `fast_execution_available`：是否允许执行需要相应成交能力的分支。

参数覆盖会进入子 run 和 comparison 的正式 manifest 与签名。刷新后可从“历史研究”恢复当次完整参数的只读事实；新建表单仍从当前发布默认值初始化。

## 7. 公共资金、成本与成交设置

### 7.1 初始资金

当前固定：

```text
currency = CNY
money_scale = 100
```

页面输入值直接作为 `initial_cash_i64`。因此：

- 输入 `100000000` 表示 1,000,000.00 CNY。
- 输入 `100000` 表示 1,000.00 CNY。

不要把“元”数值误当成 `i64` 最小货币单位，否则资金会缩小 100 倍。

### 7.2 每手手续费

当前使用 `fixed_per_contract`：

- 页面输入值是每手单边手续费的 `amount_i64`。
- `money_scale=100` 时，输入 `350` 表示每手每次成交 3.50 CNY。
- 开仓和平仓分别收费。

### 7.3 滑点

- 单位是 tick。
- 输入 `1` 表示按成交方向让价 1 个最小价格变动单位。
- 滑点最终折算到每笔交易和总成本。

### 7.4 合约乘数

使用真实合约的每点货币价值。错误乘数会直接同比例放大或缩小盈亏、保证金和风险损失。

### 7.5 保证金率

- 输入小数，例如 `0.1` 表示 10%。
- 应按所研究时期和品种的实际保证金口径设置。
- 不同策略比较必须使用同一保证金率，才能保持横向可比。

### 7.6 固定成交时点

策略研究当前固定使用：

- 信号在当前 K 线收盘后产生：`signal_timing=bar_close`。
- 默认下一根 K 线开盘成交：`fill_timing=next_bar_open`。
- 同根冲突采用最不利规则：`intrabar_conflict_rule=worst_case`。
- 随机种子固定为 `20260822`。

## 8. 统一风险覆盖参数

展开“统一风控参数”可编辑批次共用的风险规则。比例统一使用 ppm：

```text
1,000,000 ppm = 100%
100,000 ppm = 10%
10,000 ppm = 1%
```

| 参数 | 默认值 | 含义 |
|---|---:|---|
| `leverage_allowed` | `false` | 默认禁止杠杆 |
| `leverage_approval_id` | 空 | 开启杠杆时必须提供独立批准编号 |
| `max_position_weight_ppm` | 100000 | 单标的最大持仓权重 10% |
| `max_sector_weight_ppm` | 300000 | 单板块最大权重 30% |
| `max_order_loss_weight_ppm` | 10000 | 单笔压力损失上限 1% |
| `stress_loss_per_contract_i64` | 100000 | 每手压力损失的定点金额 |
| `max_daily_loss_ppm` | 20000 | 单日亏损熔断阈值 2% |
| `max_strategy_drawdown_ppm` | 150000 | 策略回撤熔断阈值 15% |
| `max_order_participation_ppm` | 100000 | 最大成交量参与率 10% |
| `max_stale_bars` | 0 | 允许的陈旧 K 线数 |
| `max_data_gap_bars` | 0 | 允许的数据缺口数 |
| `max_open_signal_age_bars` | 3 | 开仓信号最多顺延 K 线数 |
| `event_risk_max_position_weight_ppm` | 50000 | 事件风险期间最大持仓权重 5% |
| `kill_switch_on_data_revision` | `true` | 运行中数据 revision 改变时熔断 |

风险层只能批准、减量或阻断策略订单，不能增加策略原本要求的仓位。建议首次比较使用发布默认值，之后每次只改变一组风险参数，并创建新的 comparison。

## 9. 最少交易数

默认值为 20。该值进入 comparison 签名，并由 Python 聚合器用于低样本分层；它仍不代表统计显著性。

因此当前页面中：

- `trade_count < 20` 不会自动隐藏策略。
- 不会自动标记“低样本”。
- 不应把少量交易产生的高收益直接视为有效策略。

该值将在 11B 中用于“盈利但样本不足”和“样本达标盈利候选”的分层。

## 10. 开始、观察和取消批次

确认设置后，点击：

```text
一键回测所选 N 个策略
```

进度区显示：

- comparison ID。
- 当前批次状态。
- 已处理数/总数。
- 当前策略算法 ID。
- 失败策略数。

状态含义：

| 状态 | 含义 | 应对方式 |
|---|---|---|
| `queued` | 已创建，等待执行 | 正常等待 |
| `running` | Python 正在顺序执行策略 | 不要关闭服务 |
| `cancelling` | 已请求取消 | 等待终态 |
| `completed` | comparison 已成功聚合并落盘 | 仍需检查 `failed_count` |
| `failed` | 批次基础设施或提交失败 | 查看页面错误与三端日志 |
| `cancelled` | 已取消 | 需要时重新创建新批次 |
| `interrupted` | 服务重启导致中断 | 当前不能续算，只能重新运行 |

`completed` 不等于所有策略都成功。单个策略失败会写入 `results.json`，其余策略继续运行。

运行中可点击“取消”。取消不会覆盖已经存在的正式 run。

## 11. 查看当前基础结果

comparison 完成后，当前页面显示：

- 策略名称。
- 子策略状态。
- 总收益。
- 最大回撤。
- 交易次数。
- 成功子 run ID 或失败原因。

阅读顺序建议：

1. 先看 `status` 和失败原因。
2. 再看 `trade_count`；零交易或极少交易不能据此判断策略优劣。
3. 对比 `total_return` 时同时查看 `max_drawdown`。
4. 记录 run ID，使用单策略回测 API 查看正式明细。
5. 检查手续费和滑点是否符合真实市场条件。

研究页支持指标排序与筛选、互斥分层、Pareto 散点图、点击 run 查看详情/加载主图，以及最多 5 个策略的归一化权益与回撤叠加。

## 12. 结果文件位置

### 12.1 comparison 目录

```text
<data_root>/comparisons/<comparison_id>/
├── comparison.json
├── results.json
└── _SUCCESS
```

- `comparison.json`：数据集、revision、区间、执行、资金、风险和计数口径。
- `results.json`：逐策略状态、参数、run ID、签名、summary 或错误。
- `_SUCCESS`：存在时才表示 comparison 已原子提交。

### 12.2 正式 run 目录

每个成功策略生成：

```text
<data_root>/runs/<run_id>/
```

其中包含 `run.json`、`summary.json`、策略状态、阶段信号、交易信号、风险决策、图表事件、订单、成交、交易、持仓、权益、正式日志和 `_SUCCESS`。

完成的 run 不可覆盖。不要手工修改 run 或 comparison 文件，否则签名、revision 和审计链会失去意义。

### 12.3 备份

建议备份：

- `history`
- `config`
- `catalog`
- `normalized`
- `runs`
- `comparisons`
- `studies`
- `workspaces`
- `tasks/jobs`

`cache` 可以重建，不能替代正式 run 或 comparison 备份。

## 13. 使用公共 API

页面已经封装了以下接口：

```text
POST /api/v1/strategy-comparisons
GET  /api/v1/strategy-comparisons
GET  /api/v1/strategy-comparisons/{comparison_id}
POST /api/v1/strategy-comparisons/{comparison_id}/cancel
GET  /api/v1/strategy-comparisons/{comparison_id}/results
```

示例：读取当前数据集的历史 comparison：

```powershell
Invoke-RestMethod `
  -Uri 'http://127.0.0.1:8080/api/v1/strategy-comparisons?dataset_id=SHFE.AO2609.5m' `
  -Method Get
```

示例：读取状态：

```powershell
$comparisonId = 'comparison-替换为实际ID'
Invoke-RestMethod `
  -Uri "http://127.0.0.1:8080/api/v1/strategy-comparisons/$comparisonId" `
  -Method Get
```

示例：读取逐策略结果：

```powershell
Invoke-RestMethod `
  -Uri "http://127.0.0.1:8080/api/v1/strategy-comparisons/$comparisonId/results" `
  -Method Get
```

创建请求必须使用 `GET /api/v1/algorithms` 返回的精确 `algorithm_version` 和 `source_hash`，不能手写过期版本。完整契约见 `contracts/openapi.yaml`。

## 14. 常见问题

### 14.1 按钮不可点击

检查：

- 是否已经选择就绪数据集。
- 是否至少勾选一个正式策略。
- 算法目录中是否存在 `unified_risk_execution_overlay`。
- 是否已有批次处于运行或取消中。

### 14.2 某个策略失败，但批次显示 completed

这是失败隔离的正常行为。查看失败行或 `results.json` 中的 `error.code`、`error.message`。成功策略的 run 仍然有效。

### 14.3 策略显示零交易

零交易不等于程序失败。可能原因包括：

- 当前历史区间没有形成该策略要求的已确认结构。
- 策略只允许特定方向。
- 风险层阻断全部开仓。
- 成本、成交量或执行能力参数使分支失效。
- 固定级别投影与当前行情结构不匹配。

可先用单策略回测查看阶段信号、交易信号和风险决策，区分“没有信号”“信号被过滤”和“订单未成交”。

### 14.4 收益很高但交易很少

先按低样本处理。当前“单周期可靠性研究”允许选择 1–32 个同周期数据集：只选一支票也可以完成研究，但只能形成探索性证据；可靠候选仍要求至少三个独立品种组。默认使用 252/63/63 交易日滚动走步：勾选参与选参的参数并填写候选值后，每折只用训练窗排名，随后显示样本外收益、回撤、成交数、盈利折比例和参数漂移；AO2609/AOL9 等同一独立组先组内等权。勾选执行与成本压力测试后，会冻结每折参数并显示基准、1.5/2 倍成本、额外 1/2 tick、额外一根 K 线延迟和 10% 成交量参与率场景。统计验证默认对独立组等权的样本外日收益执行固定种子的 5 日区块 bootstrap 2,000 次，并展示 95% 置信区间、Holm 多重比较警告、相邻参数档位通过率、证据矩阵和未通过原因。

### 14.5 年化收益为空

当前回测 summary 的 `annualized_return` 可能为 `null`。系统先按 `trading_day` 取每日收盘权益生成日收益，再按 252 个交易日口径计算年化收益、日 Sharpe 和波动率；日样本不足或方差为零时会同时返回稳定的 reason。不要把分钟收益直接套用 `sqrt(252)`。

### 14.6 刷新后看不到刚才的比较结果

页面会列出当前数据集的历史 comparison；点击即可恢复。算法版本、source hash 或数据 revision 已变化时显示“已失效（只读）”，不会用新默认参数替换旧事实。

### 14.7 服务重启后任务变成 interrupted

这是设计行为。运行中的任务不会跨进程续算；确认数据 revision 和算法版本仍有效后重新提交。

## 15. 推荐研究流程

1. 固定一个数据集 revision。
2. 使用全部策略发布默认参数跑基线 comparison。
3. 排除失败项，单独检查零交易或异常高收益项。
4. 对有交易的策略核对交易成本和风险阻断。
5. 只改变一组公共成本或风控参数，创建新的 comparison。
6. 对候选策略使用单策略回测检查交易和图表事件。
7. 再使用“优化”页进行显式训练—验证，不在同一历史样本上反复挑最好参数。
8. 在“同周期多数据集研究”查看统计证据和认证等级；只有历史数据门禁全部通过才可能显示“可靠候选”。
9. 在自动交易前仍需完成独立的影子运行和交易基础设施工程。

当前工作台已完成单周期历史可靠性证据链，但任何认证都不代表未来必然盈利，也不能单独作为自动交易上线依据。
