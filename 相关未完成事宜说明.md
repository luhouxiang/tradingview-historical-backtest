# 相关未完成事宜说明

> 2026-08-26 更新：第 4–7 节列出的 11A-H、11B、11C、11D 缺口和第 9 节的单周期研究严谨性（12A–12E）已经完成，原文保留作实施审计依据。第 8 节递归多级别和第 10 节自动交易独立工程仍未完成，且不属于本次范围。

版本：1.0  
整理日期：2026-08-23  
适用工程：`tradingview-historical-backtest`

## 1. 文档目的

本文集中登记两类尚未完成的工作：

1. 本次“缠论策略比较与研究工作台”计划中 11B、11C、11D，以及 11A 仍需补强的部分。
2. 前面“阅读 108 课算法集并依次结合本工程实现”Goal 中，因固定级别范围或多级别依赖而未完成的算法能力。

本文是后续重新开工时的任务入口。实现时仍需遵守根目录 `AGENTS.md`：先读 README、DECISIONS 和当前里程碑文档；先改契约；每次只实现并验收一个明确里程碑。

## 2. 当前状态快照

### 2.1 Goal 状态

截至 2026-08-23，Goal 服务没有活动 Goal。本文所称“前面 Goal”依据以下已存在记录重建：

- 用户原始目标：阅读 `docs/chanlun_108_algorithm_set_20260819/README.md`，将整理出的功能依次结合本工程实现，要求算法可测试、可展示在图表上。
- `README.md` 当前状态。
- `DECISIONS.md` 的 C-057 至 C-062。
- `docs/09-implementation-roadmap.md`。
- `docs/chanlun-algorithm-coverage.json` 的逐项审计结果。

因此，后续若重新创建 Goal，应以本文的里程碑顺序为目标，而不是假定存在一个仍在后台运行的旧 Goal。

### 2.2 已完成，不应重复实现

- 里程碑 0–9 已有数据、图表、指标、回放、正式回测、任务持久化和参数优化基础设施。
- 缠论里程碑 10 已完成至 10AB。
- 108 课目录共 27 个算法项：
  - 14 项为 `implemented`。
  - 11 项为 `fixed_level_projection`。
  - 0 项为 `partial`。
  - 2 项为 `deferred`。
- 当前固定级别范围内，已有策略状态、阶段信号、交易信号、订单、成交、交易、权益、风险决策、对象树和主图事件。
- 里程碑 11A 已有基线能力：
  - 算法目录发布研究元数据。
  - 默认发现 15 个正式策略（13 个基础策略和 2 个 MACD 组合策略）。
  - 同一批次顺序执行策略。
  - 批次内共享一次进程内缠论计算。
  - 单策略失败隔离。
  - 成功策略生成正式子 run。
  - comparison 和子 run 落盘。
  - Go 提供创建、列表、状态、取消和结果 API。
  - Vue 提供策略选择、参数覆盖、公共资金/成本/风控和基础结果。

### 2.3 尚未达到的最终业务目标

当前已经“能运行策略”，但还没有完整回答用户真正关心的三个问题：

1. 哪些策略在统一口径下更可能有用，而不是偶然由极少交易产生高收益。
2. 哪些策略在收益与回撤之间更占优，以及差异来自哪些参数、成交或风险约束。
3. 每个策略究竟在什么缠论行情结构下赚钱或亏钱，并能定位到具体交易和图表原因。

所以，策略代码并非完全没做；主要缺口是研究聚合、比较展示、交易归因和递归多级别语义。

## 3. 建议后续实施总顺序

| 顺序 | 里程碑 | 目标 | 是否可放缓 |
|---:|---|---|---|
| 1 | 11A-H | 补齐 comparison 可恢复性、签名和依赖留档 | 不建议放缓 |
| 2 | 11B | 排行榜、分层、Pareto、历史研究恢复 | 不建议放缓 |
| 3 | 11C | 单策略拆解和 2–5 策略图表对比 | 不建议放缓 |
| 4 | 11D | 开仓时点缠论行情条件归因 | 不建议放缓 |
| 5 | 10-ML1 | 递归结构级别补全 11 个固定级别投影 | 用户已允许放缓 |
| 6 | 10-ML2 | `ALG-STR-006` 区间套 | 用户已允许放缓 |
| 7 | 10-ML3 | `ALG-STR-010` 核心/机动双账本 | 用户已允许放缓 |
| 8 | 12 | 走步、跨数据集、成本敏感性和影子运行 | 自动交易前必须完成 |
| 9 | 独立项目 | 模拟柜台、实时行情和实盘执行 | 当前明确不在范围内 |

建议使用已完成的 11A-H、11B、11C、11D，先判断当前 13 个基础策略及 2 个 MACD 组合策略是否值得继续投入；多级别仍可按既定范围放缓。

## 4. 11A 遗留补强：comparison 研究事实尚未完全闭环

虽然 11A 主链已运行，但下列内容仍应作为 11A-H 小里程碑补齐，不能等到自动交易阶段再处理。

### 4.1 comparison manifest 尚未保存完整策略引用

当前 `comparison.json` 保存数据集、区间、执行、资金、风控、随机种子和计数，但没有在 manifest 内完整保存：

- 每个策略的 `kind`。
- `algorithm_id`。
- `algorithm_version`。
- `source_hash`。
- 每个策略的完整规范化参数。
- 参数相对发布默认值的覆盖差异。

`results.json` 虽保存算法 ID 和参数，成功项可通过子 run 找回版本，但失败项没有正式子 run，不能只靠当前 comparison 目录完整重建当时算法身份。

需要实现：

1. 扩展 comparison manifest Schema。
2. 在 Go 预校验后把规范化策略引用和参数写入 Python payload。
3. Python 原样写入不可变 manifest。
4. 对失败策略也保留完整算法引用。
5. 增加旧 manifest 兼容读取规则。

验收：仅凭 comparison 目录即可无歧义回答“当时跑了哪个算法版本和哪些完整参数”。

### 4.2 缺少 comparison 总签名

当前每个子 run 有 `run_signature`，但 comparison 没有覆盖整批请求的统一签名。用户改变一个策略参数、公共成本或风控参数后，系统无法用一个稳定值判断两个批次是否完全同口径。

需要实现 `comparison_signature`，至少覆盖：

- dataset ID 和 revision。
- 回测及预热区间。
- 有序策略引用和规范化完整参数。
- 执行、资金、手续费、滑点、乘数和保证金。
- 风险覆盖算法、参数与 context。
- 随机种子。
- 最少交易数。
- Python engine 版本和 comparison 聚合版本。

验收：完全相同请求产生相同签名；任何影响结果或分类的字段变化都改变签名。

### 4.3 共享缠论依赖目前只存在于进程内

当前批次通过 `_shared_chan_cache` 在一个 Python 进程内只计算一次缠论结构，已经避免 13 次重复计算，但尚未实现原计划中的“已验证缠论事件依赖引用”。

缺口：

- 没有独立的共享依赖 manifest。
- 没有 dependency hash。
- 没有 `_SUCCESS` 引用校验。
- 中断后不能从已完成的共享依赖恢复。
- 子 run manifest 没有记录本批次消费的共享依赖身份。

需要实现：

1. 由 Go 解析并验证 `data_root` 内共享缠论缓存引用。
2. 校验 dataset/revision、缠论算法版本、source hash、参数、内容 hash 和 `_SUCCESS`。
3. Python 策略入口允许消费只读依赖引用。
4. 独立计算与共享依赖计算的信号、成交、权益逐项一致。
5. 仍保持 Python 为唯一权威算法实现，Go 不解析缠论事件。

### 4.4 取消批次后的已完成子 run 可能成为孤立文件

当前 comparison 在取消或中断时会删除批次临时目录；已经先完成的子 run 是正式不可变目录，但父批次尚未提交时，Go 可能没有把它们登记成可通过普通回测 API 读取的任务。

需要决定并实现一种明确语义：

- 方案 A：取消时保留并登记所有已经 `_SUCCESS` 的子 run，同时写一个 cancelled comparison manifest；或
- 方案 B：comparison 在每个子 run 完成时持久化增量 journal，重启后可恢复父子关系。

不得静默删除已经成功提交的正式 run，也不得让未知孤儿目录长期不可发现。

### 4.5 运行中 comparison 不能恢复续算

当前服务重启后，`queued/running/cancelling` 会变为 `interrupted`，用户只能重新提交。完成历史可由 API 扫描，但运行状态不能恢复。

短期可以保持“不续算”，但应做到：

- 中断批次有可读 manifest 或 journal。
- 明确列出已经完成和尚未开始的策略。
- 允许用户选择“重新运行剩余项”并创建新 comparison。
- 新 comparison 引用前一个 comparison ID，而不是静默覆盖。

### 4.6 11A-H 测试缺口

需要增加：

- 完整算法引用与参数的 Schema/example/契约测试。
- comparison 签名稳定性和字段敏感性测试。
- 共享依赖路径逃逸、revision、版本、hash 和 `_SUCCESS` 拒绝测试。
- 取消发生在第 1 个、第 N 个和最后一个策略时的父子 run 一致性测试。
- 全部策略失败时的 comparison 终态测试。
- 持久任务管理器重启后读取 completed comparison 和子 run 的集成测试。

## 5. 11B：排行榜、分层、Pareto 与历史研究恢复

这是当前最优先的用户价值里程碑。没有 11B，系统虽然可以一键跑完，却仍需人工逐行抄结果，无法快速判断策略是否值得深入研究。

### 5.1 指标口径先决工作

排行榜需要统一并固化：

- 总收益。
- 年化收益。
- 最大回撤。
- Sharpe。
- Profit Factor。
- 胜率。
- 平均期望。
- 交易次数。
- 手续费。
- 滑点。
- 总交易成本。

当前已知问题：

- `annualized_return` 当前为 `null`，原因为 `timeframe_annualization_not_configured`。
- Sharpe 当前保存 `sharpe_annualization_factor=252`，但需要确认它是否适用于分钟 K 线收益序列；不能把日频 252 直接套到任意 K 线周期。
- `expectancy_i64`、手续费和滑点为定点金额，展示时必须按 `money_scale` 转换。
- `profit_factor=null` 可能表示没有亏损交易，不能与零或缺失混为一类。
- 期末未平仓浮盈亏会进入权益和总收益，但不能进入已平仓胜率和条件 realized PnL。

需要先建立 Python 唯一权威的指标语义和测试，Go/Vue 只读取和展示。

### 5.2 Python comparison 聚合器

新增独立聚合入口，负责：

1. 校验所有成功子 run 属于同一 comparison 口径。
2. 读取 summary，而不是由 Go 重算绩效。
3. 处理零交易、空值、无亏损交易、部分成交和期末持仓。
4. 计算结果分层。
5. 计算收益—回撤 Pareto 前沿。
6. 输出稳定、版本化的比较结果 Schema。

分层必须是互斥且可解释的：

1. `no_trades`：无交易。
2. `loss_making`：总收益为负。
3. `profitable_low_sample`：盈利但交易数低于阈值。
4. `profitable_candidate`：盈利且样本达到阈值。
5. `pareto_candidate`：在收益最大化、最大回撤最小化上不被其他合格候选支配。

不生成主观综合分，不把不同量纲随意加权。

### 5.3 排行榜 UI

需要实现：

- 按任意指标升降序排序。
- 按策略名称和策略族过滤。
- 隐藏失败项。
- 隐藏零交易项。
- 显示低样本警告。
- 明确空值含义。
- 固定列或横向滚动，避免十多个指标不可读。
- 显示参数是否偏离发布默认值。

### 5.4 收益—回撤散点图

- X/Y 轴必须标清收益和最大回撤的正负方向。
- 点大小表示交易次数，但要设置最小/最大半径，避免极端样本吞没其他点。
- 颜色表示结果分层。
- Pareto 点有独立边框，不使用“综合第一名”措辞。
- 悬停显示策略、收益、回撤、交易数、成本和 run ID。
- 点击点与排行榜行联动。

### 5.5 历史研究恢复

Go 已有 completed comparison 列表 API，但 Vue 尚未调用。需要增加：

- 当前数据集的历史研究列表。
- 刷新页面后恢复 comparison 结果。
- 按创建时间、数据 revision 和签名区分批次。
- 算法版本或 source hash 已变化时标记 `stale/invalidated`。
- 不能用新版本默认参数静默替换历史参数。
- 工作区保存手工策略选择和参数覆盖。

### 5.6 11B 验收

- 零交易、全亏损、低样本、达标盈利和 Pareto 五类夹具通过。
- 排名和 Pareto 对空值、相同点、负收益、零回撤有确定行为。
- 刷新浏览器可恢复完成历史。
- 修改任一算法版本后历史批次明确失效，但仍可只读查看旧事实。
- Python、Go、Vue、契约和 E2E 门禁通过。

## 6. 11C：单策略拆解与 2–5 策略对比

### 6.1 单策略详情

当前普通 backtest API 已能读取 summary、交易、权益和图表事件，但策略研究页尚未把 comparison 子 run 接到这些能力。

需要增加：

- 点击排行榜行进入详情。
- 汇总指标卡。
- 交易明细表和分页。
- 权益曲线。
- 回撤曲线。
- 参数与发布默认值差异。
- 风控批准、减量、阻断和熔断统计。
- 失败策略的完整诊断信息。
- “加载到对象树和主图”操作。

研究面板需要把选中的成功子 run 作为 `StrategyRunSource` 交给 `AppShell`，复用现有主图和对象树，不在 Vue 重算交易或缠论。

### 6.2 2–5 策略对比

需要实现：

- 最多选择 5 个策略叠加。
- 权益归一化为起点 100。
- 回撤曲线叠加。
- 指标差值表。
- 参数差异表。
- 主图按策略颜色叠加买卖事件。
- 每个策略独立显隐。
- 图例、颜色和表格行保持稳定映射。

超过 5 个策略仍可留在排行榜，不允许同时叠加到图表。

### 6.3 对比口径

- 归一化只用于可视化，不改变原始资金和盈亏事实。
- 不同 run 必须属于同一 dataset/revision 和区间才能直接叠加。
- 若成本、资金、风险口径不同，应拒绝或显著警告，不得画成同口径曲线。
- 空权益、零交易和期末未平仓必须有明确展示。

### 6.4 11C 验收

- 单策略详情与普通 backtest API 数值逐项一致。
- 2–5 策略归一化权益和回撤数学测试通过。
- 第 6 个策略不能进入叠加，但仍保留排行榜选择。
- 主图信号显隐不触发 Python 重新计算。
- 点击交易可以把时间轴和主图定位到对应开平仓。

## 7. 11D：缠论行情条件归因

这是把“有策略”转化为“知道策略在什么结构下有效”的关键里程碑。

### 7.1 交易事实契约缺口

当前 `orders.parquet` 有 `signal_id`，交易信号有 `known_at_bar_index`，但 `trades.parquet` 和公共 `BacktestTrade` 尚未直接保存：

- `entry_signal_id`。
- `entry_signal_known_at_bar_index`。
- 可选的 `exit_signal_id`。
- 交易与入场批次/订单的明确关联。

现有 trade 只能通过 bar 和订单间接推断，无法满足无歧义条件归因。

需要先改 contracts、Python Parquet Schema、Go 读取类型、Vue 类型和兼容测试。旧 run 保持可读，但标记 `attribution_supported=false`。

### 7.2 开仓时点状态重建

必须使用开仓信号的 `known_at_bar_index`，只读取当时已经可见的因果事件，禁止使用：

- 退出时状态。
- 最终全历史对象快照。
- 后续 revision 回填到较早时点。

每笔已平仓交易至少记录：

- L0 走势分类：上涨趋势、下跌趋势、盘整、高级别中枢候选、未知。
- 中枢阶段：盘整、中枢震荡、向上迁移、向下迁移、未知。
- 开仓价相对关联中枢：上方、内部、下方、未知。
- 触发类别：B1/B2/B3、S1/S2/S3、类买卖点、其他。
- 关联结构对象 ID、revision 和 reason code。

### 7.3 条件聚合

按“策略 × 条件维度 × 条件值”计算：

- 已平仓交易次数。
- 胜率。
- realized 净盈亏。
- 平均期望。
- Profit Factor。
- 平均持仓 K 线数。
- 手续费和滑点。

约束：

- 条件净盈亏只统计已平仓交易。
- 所有条件桶 realized PnL 加总必须与 run 已平仓 realized PnL 对账。
- 期末未平仓浮盈亏单独显示。
- 未知状态不能丢弃，必须进入 `unknown` 桶。
- 同一交易在同一维度只能进入一个互斥桶。

### 7.4 热力图和反向定位

- 行：策略。
- 列：行情条件。
- 颜色：平均期望。
- 透明度或纹理：样本量。
- 显示样本数，防止少量交易以高饱和颜色误导。
- 点击单元格筛选具体交易。
- 点击交易定位主图开仓、平仓、触发信号和关联缠论结构。

### 7.5 前缀不变性验收

必须构造测试证明：追加未来 K 线或后续结构 revision，不改变历史交易开仓时点已经记录的行情分类。该测试是 11D 的数据正确性红线。

## 8. 前面 108 课 Goal 的多级别未完成项

### 8.1 总体结论

`docs/chanlun-algorithm-coverage.json` 当前为：

```text
catalog_items = 27
implemented = 14
fixed_level_projection = 11
partial = 0
deferred = 2
catalog_complete = false
```

这表示：固定级别主链已经可测试、可回放、可上图，但严格递归多级别口径并未全部完成。不能把固定 K 线周期、多个时间周期拼接或旧 `analysis_level=segment` 冒充缠论结构级别。

用户已经明确“多周期可以放缓”，因此本节优先级低于 11B–11D，但必须保留，不得把审计状态改成全部完成。

### 8.2 11 个固定级别投影

| 目录 ID | 当前能力 | 尚未完成的严格多级别语义 |
|---|---|---|
| `ALG-SIG-002` | 固定确认组件上的盘整背驰 | 可比较的递归次级别震荡走势 |
| `ALG-SIG-003` | 固定线段对象图上的二、三类点分支 | 真正逐层级的 B2/B3 合并、回试与走势完成 |
| `ALG-SIG-004` | 固定线段离开及首次回试 | 递归次级别离开走势和第一次完成回试 |
| `ALG-STR-002` | B2 后第一条确认线段跟随 | 递归低级别第一段上涨/回试/背驰链 |
| `ALG-STR-003` | 固定线段与中枢迁移图 | 多级别第三点后的递归跟随与趋势持有 |
| `ALG-STR-004` | 中枢内部确认组件与 Zn 投影 | 递归次级震荡走势、背驰和末中枢分支 |
| `ALG-STR-007` | `segment_center_chain_v1` 三层投影 | 真正的 L0/L1/L2 递归分类和高级别确认迁移 |
| `ALG-STR-008` | `segment_rebound_rhythm_v1` 固定反弹节奏 | 目标级别与 L-1 级递归分段操作 |
| `ALG-STR-009` | 固定线段 B1/S1—中枢—B3/S3 | 精确递归底顶构造；粗略分型分支仍保持非交易 |
| `ALG-AUX-003` | 已确认趋势背驰到结构解决的 BARDO 投影 | 公共递归 BARDO 对象与跨级别结束/升级 |
| `ALG-AUX-004` | 中枢组成线段映射为次级震荡区间 | 递归次级中枢、末中枢在外和趋势型预警 |

### 8.3 多级别基础设施应先于策略

补全上述投影时，建议建立独立里程碑 10-ML1：

1. 明确定义结构级别 ID，不绑定 K 线周期。
2. 每一级对象都保存稳定 ID、父子引用、candidate/confirmed/invalidated 和 revision。
3. 中枢与走势可由下一层已确认对象递归组成。
4. 形成显式的 level graph，不允许策略扫描原始 K 线私建第二套结构。
5. Parquet、因果事件、Go API、回放、对象树和主图同步支持。
6. 增加至少三层的正例、反例、等号、修订和前缀不变性测试。

只有 10-ML1 完成后，才能把 11 个 `fixed_level_projection` 逐项升级为 `implemented`。

### 8.4 延期项一：`ALG-STR-006` 区间套精确定位

当前缺失目录 emits：

- `nested_turn_location`
- `entry_after_confirmation`

依赖：

- 高级别走势已经进入有效背驰段。
- 在背驰段内部递归寻找下一层同向背驰段。
- 每一层都使用结构级别，不是简单切换 30m/5m/1m 周期。
- 最小执行层转折确认后，下一可交易时点成交。
- 任一层来源 revision 变化时因果失效。

需要实现：

1. 区间套路径对象和稳定 ID。
2. 每层进入、退出、失效原因。
3. 数据粒度、手续费、滑点和流动性停止递归条件。
4. 策略事件、正式 run、对象树和主图定位。
5. 共享与独立计算一致、前缀不变性和真实图例验收。

### 8.5 延期项二：`ALG-STR-010` 大级别底仓与次级别短差

当前缺失目录 emits：

- `core_hold`
- `swing_reduce`
- `swing_add_back`
- `core_exit`

该策略不能用当前单向净持仓的一套成本和盈亏记录冒充完成。需要：

1. 明确大级别核心仓与次级别机动仓的独立 lot/book 身份。
2. 核心仓只由大级别买卖点控制。
3. 机动仓由次级别 S1/B1 控制减仓和回补。
4. 核心/机动比例外置并进入签名。
5. 成交、手续费、滑点、realized/unrealized PnL 分账。
6. 汇总时同时给出分账指标和组合指标，禁止用核心仓浮盈掩盖短差亏损。
7. 主图和对象树区分核心仓与机动仓事件。
8. 与 11D 条件归因兼容。

## 9. 策略研究严谨性完成审计

以下能力已由 12A–12E 补齐；完成历史研究门禁仍不等于可以直接进入自动交易。

### 9.1 多数据集研究

12A 已完成前置基础：完整历史导入不再被图表窗口裁剪，支持批量导入、精确交易日计数、独立组、同组重叠检测和探索级/认证就绪数据门禁；AO2609/AOL9 会归入同一独立组。12B 已完成同周期多数据集不可变 Research Study。12C 已完成默认 252/63/63 交易日滚动走步、严格训练窗选参、样本外日收益与折级指标、参数漂移、失败原因和正式子 run 留档。12D 已完成冻结参数的基准、成本倍数、额外滑点、延迟及 10% 成交量参与率压力子 run，并提供收益、回撤、成交率、边际退化和首个失效场景。12E 已完成固定种子 5 日区块 bootstrap、Holm 校正、相邻参数档位扰动、三级历史认证、证据界面和真实数据 E2E。

当前一个 comparison 只包含一个数据集。后续至少需要：

- 同策略跨合约、跨年份和跨市场验证。
- 每个数据集独立 revision 和结果，不把 K 线直接拼接成伪连续序列。
- 分数据集指标、总体汇总和最差数据集表现。
- 防止只挑对策略有利的品种。

### 9.2 走步验证

现有优化支持固定训练/验证区间和 grid/seeded random，但没有 walk-forward：

- 滚动训练窗口。
- 紧随其后的验证窗口。
- 参数只能由当时训练数据选择。
- 多折稳定性和参数漂移。
- 禁止使用后续窗口反选前面参数。

### 9.3 成本敏感性

对候选策略至少运行：

- 基准手续费/滑点。
- 成本上浮情景。
- 低流动性成交量参与限制。
- 不同保证金率。
- 延迟 1 根或多根 K 线成交。

若策略只在零手续费或零滑点下盈利，应明确判为不可执行候选。

### 9.4 样本量和稳健性

默认 20 笔只用于低样本警告，不是统计显著性证明。后续应补：

- 盈亏分布和尾部损失。
- 连续亏损长度。
- 时间分段稳定性。
- 参数邻域稳定性。
- bootstrap 或其他明确方法的置信区间；方法和假设必须留档。
- 多重比较带来的“策略越多越容易挑出偶然赢家”风险提示。

### 9.5 AOL9 完整研究 E2E

原有 comparison 的 AOL9 流程已经覆盖；12E 进一步使用完整 `DCE.YL9.5m` 与 `SHFE.AOL9.5m` 执行真实 Research Study：

```text
导入完整 YL9/AOL9 5 分钟历史
→ 252/63/63 走步选择参数
→ 冻结参数执行 7 个压力场景
→ 执行相邻档位扰动和 2,000 次区块 bootstrap
→ 汇总证据矩阵并分级认证
→ 中断后从 journal 恢复同一不可变 Study
```

验收完成 17 个验证折及其训练、压力和邻域子 run。由于只有两个独立品种组且该策略样本外无已平仓交易，结果被正确限制为“探索性”，没有误授予“可靠候选”。摘要见 `trading-data/acceptance/milestone12-real-e2e.json`。

## 10. 自动交易前仍缺失的独立工程

本仓库当前明确只研究历史回测，不包含实时行情、模拟柜台或实盘下单。以下不是本轮里程碑遗漏，而是未来必须单独立项的系统：

- 实时行情接入与断线恢复。
- 交易时段、合约切换和主力/连续合约治理。
- 模拟柜台和 paper trading。
- 券商/期货公司交易适配器。
- 委托回报、撤单、部分成交和拒单状态机。
- 实盘持仓与本地状态对账。
- 交易前风控、人工审批和紧急停止。
- 幂等下单和灾难恢复。
- 实时监控、告警、审计和权限管理。
- 影子运行：实时产生信号但不下单，用来验证历史/实时一致性。

这些能力完成前，不应把历史回测结果直接连接到实盘账户。

## 11. 后续每个里程碑的强制交付物

每次只做一个里程碑，并至少交付：

1. 更新后的 OpenAPI、JSON Schema、示例和生成类型。
2. Python 权威算法或聚合实现。
3. Go 校验、任务生命周期和安全路径处理。
4. Vue 展示与交互，不复制 Python 算法。
5. Python 前缀不变性、边界和对账测试。
6. Go 参数、revision、路径、取消和恢复测试。
7. Vue 排序、筛选、图表和恢复测试。
8. 对应 E2E。
9. `DECISIONS.md` 新决策。
10. `docs/09-implementation-roadmap.md` 状态更新。
11. `scripts/test-all.ps1` 全部门禁结果。
12. 已知限制和可重复样例数据说明。

## 12. 推荐的重新开工检查单

下次继续时按以下顺序执行：

- [ ] 阅读 `AGENTS.md`、`README.md`、`DECISIONS.md`。
- [ ] 阅读本文和 `使用说明书.md`。
- [ ] 读取 `docs/chanlun-algorithm-coverage.json`，确认审计未被旧分支覆盖。
- [ ] 检查当前工作树，保护用户已有改动。
- [ ] 选择且只选择一个未完成里程碑；递归多级别或自动交易必须先由用户明确扩大范围。
- [ ] 先更新 contracts，再改 Python、Go、Vue。
- [ ] 对旧 run/comparison 保持可读，不静默迁移或覆盖。
- [ ] 使用固定 Python 3.14 解释器执行测试。
- [ ] 运行 `scripts/test-all.ps1`。
- [ ] 只有验收全部通过后，才更新“已完成”状态。

## 13. 完成判据

“策略研究工作台完成”至少意味着：

- 一键批量回测事实可完整重建。
- 排行和 Pareto 由 Python 权威聚合且口径明确。
- 页面可恢复历史研究。
- 单策略和多策略图表对比可用。
- 每笔交易可按开仓时点的缠论状态归因。
- 条件 realized PnL 与正式 run 对账。
- AOL9 完整流程 E2E 通过。

“前面 108 课 Goal 完成”还要求：

- 11 个固定级别投影完成真正递归结构级别实现，或经用户明确决定永久保留投影并修改 Goal 范围。
- `ALG-STR-006` 和 `ALG-STR-010` 不再处于 deferred。
- `docs/chanlun-algorithm-coverage.json` 的 `catalog_complete=true`。
- 所有目录 emits 都有实现、测试、图表证据和因果确认时点。

在此之前，应使用“固定级别范围已完成、完整递归多级别仍未完成”的准确表述。
