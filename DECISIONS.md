# 架构决策与需求状态

## A. 用户已确认的硬约束

| ID | 决策 |
|---|---|
| C-001 | 主体界面尽可能参照 TradingView。 |
| C-002 | 图表内有一张主图和两张或更多副图；默认主图 : 副图1 : 副图2 = 6:1:1。 |
| C-003 | 主副图及副图之间可拖拽调整，相邻两窗格一增一减。 |
| C-004 | 图表区与右侧对象区域可拖拽调整宽度，右侧区域不得覆盖图表。 |
| C-005 | K 线、均线、缠论笔/中枢、用户矩形必须有明确图层与对象树模型。 |
| C-006 | 第一阶段只做历史浏览、指标、回放和回测。 |
| C-007 | 第一阶段不使用数据库，使用目录与文件。 |
| C-008 | 原始历史数据通常一文件对应一个合约的一个周期。 |
| C-009 | Go 使用 go1.25.7；Python 使用 3.14；前端使用 Vue 3。 |
| C-010 | Go、Python、Vue 3 都必须记录数据获取、流转、策略、阶段信号和交易信号日志。 |
| C-011 | 日志必须包含真实业务文件名和代码行号。 |
| C-012 | 每个滚动日志文件不超过 50 MiB，总文件数不超过 10。 |
| C-013 | 指标算法、策略组合和调优逻辑必须分离，调优接口要便于未来由 AI 调用。 |
| C-014 | 缠论不能随屏幕可视范围重算；结果属于合约、周期、算法版本和参数。 |
| C-015 | 缠论首版不实现线段，只计算并展示分型、笔和中枢；不得用微型笔或其他对象冒充线段。 |
| C-016 | 所有可拖拽布局分隔线使用红色 1 px 视觉线并保留至少 8 px 命中热区；图表与右侧面板之间不得为命中热区额外保留可见空白栏。 |
| C-017 | MACD 副图以 0 轴为纵向中线，正负范围对称并保留上下边距；MACD 柱使用从 0 轴出发的 1 px 红/蓝绿竖线。主图、MACD 和成交量窗格左上角显示参数与当前值并随十字线更新；均线、DIFF、DEA 使用 1 px 线宽，成交量最高柱不得触碰或越过窗格上边界。 |
| C-019 | 国内期货图表只显示一组随鼠标像素位置自由移动的十字线，不将十字线吸附到 K 线；鼠标位于某根 K 线的横向范围内时，主图 OHLC、均线、MACD 和成交量图例统一显示该根 K 线索引对应的值。底部时间标签显示本地时区下完整 K 线区间与星期（例如 `2026/07/31 21:25~21:30 五`），并使用红色背景。 |
| C-020 | MACD 柱值采用国内期货软件常见定义 `2 × (DIFF - DEA)`。非文本输入状态下直接敲击可打印字符时，右下角弹出键盘精灵；候选来自 Go 扫描 `data_root/history` 后发布的源文件，支持代码、名称和路径的大小写无关模糊匹配，方向键选择、Enter 导入或加载、Esc 关闭。 |
| C-021 | 缠论在用户界面中作为叠加到 K 线主图的指标提供，数据模型仍保持单个 `StrategySource`。每个新数据集默认创建一个全历史因果缠论源，主图默认显示笔和中枢、不显示分型；分型、笔和中枢的可见性继续由对象树分类开关控制。 |
| C-022 | 指标入口采用可扩展的统一指标管理器，不直接堆叠全部参数卡片。目录按“常用、全部、当前使用”分视图，支持名称/代码/分类搜索、分类筛选和本地收藏；普通指标与缠论共用目录，当前实例参数按需展开，底层仍分别保存为 `SeriesSource` 与单个 `StrategySource`。 |
| C-023 | 缠论包含关系、三独立 K 线严格分型、笔端点选择及同类分型极值替换以 `E:\work\py\kline-chart\klinechart\algo\chanlun\c_bi.py` 为权威参考。笔按至少 5 根独立 K 线及参考实现的后继分型区间规则确认，不使用 ATR 门槛；相邻笔必须首尾相接且方向严格交替。算法版本升为 2.0.0，禁止复用 1.x 缓存。 |
| C-024 | 笔中枢沿用 `kline-chart` 的“进入笔不参与计算”规则：随后三笔的严格价格交集确定矩形，第四笔必须回到该矩形后才因果确认；后续相交笔只延长时间范围、不改变前三笔确定的 ZD/ZG。离开笔作为下一轮进入笔，不复用前一中枢的参与笔，保证输出中枢时间范围不重叠。一次分型更新释放多笔时，确认时间按释放时点及前一笔单调化，中枢修订时间不得早于形成时间。该确认门槛是工程规则，不冒充原文对中枢的三段定义。算法版本升为 2.1.0，禁止复用 2.0.x 缓存。 |
| C-025 | “当前使用”指标卡的参数区右侧提供 `…` 样式入口。颜色、透明度、1–4 px 线宽、实线/虚线/点线和可见性按输出线配置；编辑先保存在弹窗草稿，只有点击“确认”才应用，取消不改变现有样式。样式属于工作区布局且不参与算法参数、缓存键或 Python 计算任务。MACD 柱继续使用固定的涨跌语义颜色，不纳入线条样式选择。 |
| C-026 | 用户新增“段”需求，取代 C-015 的“不实现线段”限制；笔中枢算法也由 `E:\work\py\algo-ui\common\chanlun\c_bi.py` 的 `compute_bi_pivots/process_down_up` 取代 C-024 的第四笔返回工程规则。段逐项参照同文件 `_NCHDUAN` 及其辅助函数，作为独立 `segment` 语义对象写入 `segments.parquet` 和因果事件流，主图默认以黄色线显示。算法版本升为 3.0.0，禁止复用 2.x 缓存；详细规则和交叉核验见 `docs/13-chan-bi-center-segment-algorithm.md`。 |
| C-027 | 标准线段中枢、背驰和一二三类买卖点按缠论 108 课固化为 Python 因果语义对象。标准线段中枢使用已确认段，且按第 18 课只接受 `ZD < ZG` 的正宽重叠；三类点的回抽段严格为离开段的下一段。缓存包含 `segment_zhongshu/divergences/trade_points` 三类 Parquet。笔中枢默认浅蓝色实线框、半透明填充和柔和阴影；标准线段中枢默认浅黄色实线框、半透明填充和柔和阴影。详细口径见 `docs/14-chan-108-segment-center-divergence-trade-points.md`。 |
| C-028 | 趋势背驰严格采用 `a+Z1+b+Z2+c`：两中枢完整外围范围必须不重叠，力度比较 `b=Z1.exit_index` 与 `c=Z2.exit_index`，不再比较进入首中枢前的 a 与 c；c 后首个已完成反向段确认端点。单中枢 `a+Z+c` 为盘整背驰并生成类一，类一后按相同次序生成类二；严格三类点若最近同向起点为类一则附加类三生命周期标记。标准和类二买卖点均分为最强（与三类点同点）、一般（不突破一类点）、最弱（突破但回试自身有盘整背驰确认）。契约新增 `signal_class`、`strength` 及六种类买卖点，算法版本升为 4.1.0，禁止复用 4.0.x 缓存。 |
| C-029 | 右侧“对象树”在每个缠论 `StrategySource` 父节点下逐项显示 Python 生成的背驰、标准/类买卖点对象，并按 `bar_index` 最近优先排列。点击具体信号只改变选中状态，不移动当前 K 线；若信号锚点已在屏幕内则以发生点与因果确认点两个圆形端点高亮。点击小锁才执行定位：对象不在当前缓存/视口时按其 K 线锚点加载有限连续历史窗口并移入屏幕。每个信号是策略父节点下的只读语义对象，不持久化为普通 `DrawingObject`。对象树拥有独立纵向滚动区域。 |
| C-030 | 参考 `chanlun_segment_strategy_codex_reference_20260804-20260805` 扩展缠论时按独立里程碑逐套落地单级别策略。排除需要跨周期或上下级别联动的 `multi_timeframe_core_plus_swing`、`centre_oscillation_swing`、`small_to_large_B2`、`accelerated_breakout_lower_level_B3`。单级别语义对象统一使用 `analysis_level=segment`、`component_kind=segment`；标准中枢继续采用 C-027 的正宽核心、完整振荡包络分离和 MACD 面积力度口径。新增走势状态与 `Z/Zn` 监视对象，均进入因果事件、对象树、图表图层和锁定定位流程。 |
| C-031 | `fixed_level_centre_decision_tree` 使用最近已确认线段中枢和收盘价划分 `inside / below_without_S3 / below_with_S3 / above_without_B3 / above_with_B3`。边界接触属于中枢内；只在关联当前中枢的严格 B3/S3 已确认后开多/开空，回到中枢或越过另一侧时平仓。状态、阶段、交易和图表事件共用逐 K 线事件入口；正式回测完成后作为 `StrategyRunSource` 进入对象树，点击只选中、小锁定位，主图只绘制状态与最终图表事件以避免阶段/交易事件重影。 |
| C-032 | `downtrend_reversal_only` 只接受 `signal_class=standard` 的已确认一买开多，并以标准一卖平多，不反手做空；`trend_divergence_reversal` 接受标准一买/一卖并在相反信号确认时平仓、反向开仓。两者都拒绝盘整背驰生成的 `class_buy_1/class_sell_1`。信号发生点保留在缠论对象中，策略对象统一锚定其 `known_at_bar_index` 对应 K 线的时间和收盘价，确保图表展示、对象树定位与实际可用信息时刻一致。 |
| C-033 | `consolidation_divergence_centre_reversion` 仅消费可追溯到 `divergence_kind=consolidation` 及其关联标准线段中枢的类一买/类一卖。入场后的首要目标是价格回到冻结核心 `[ZD,ZG]`；确认进入核心即结束回归仓。若关联中枢先确认严格 S3（底背驰多仓）或 B3（顶背驰空仓），则记录回归失败，平仓并切换到严格三类点方向。新仓确认 K 线当根不执行回归止盈，防止同一已知时刻产生无意义的开平重影。 |
| C-034 | `third_buy_centre_migration_hold` 仅由 `signal_class=standard` 的严格 B3/S3 触发，B3 开多、S3 开空。持仓期间不因类一买卖或价格触边做同级短差；任一不同于入场来源中枢的新标准线段中枢确认时退出，或在多仓标准 S1、空仓标准 B1 确认时按同级趋势背驰退出。类三标记不代替严格三类点，确保迁移持有的起点和终点均为已完成结构。 |
| C-035 | `first_centre_B3_rotation` 的单级别实现把“首中枢三类点”固化为：同一方向周期只执行首个 `signal_class=standard` 的 B3/S3；后续同向严格三类点发布 `LATER_CENTRE_THIRD_POINT_FILTERED` 状态但不下单；相反严格三类点出现后重置前一方向周期。持仓在新同级中枢、相反标准一类点或相反类一（盘整背驰）确认时退出。参考方案中“下一次级别走势不能创新高”等低级别失败规则不在本里程碑实现，也不以当前级别局部 K 线替代。 |
| C-036 | 通达信扫描发现运行配置中没有的合约时，由 Go 导入器使用带官方来源、规则版本和核验日期的内置品种规则，原子补充 `instruments.json` 与所需 `sessions.json` 模板；首个规则覆盖郑商所白糖 `SR`。`trading_calendar.csv` 只根据历史文件中实际出现的交易日及已有日历建立前序交易日映射，不按自然日简单减一天。若首个夜盘交易日没有可证明的前序交易日，源文件保持 `needs_mapping` 并报告缺失，不猜测日期。没有收录权威规则的品种同样保持待映射，禁止根据价格或名称猜合约乘数、最小变动价位和交易时段。 |
| C-037 | 原始行情只有一个持久数据源：`storage.data_root/history`，开发默认即 `trading-data/history`。仓库不再保留 `samples` 目录，也不由启动脚本从源码目录复制行情。完整历史测试只读该唯一数据源；隔离 smoke 可以复制到系统临时 data_root，但临时工作副本不构成第二个持久数据源。 |
| C-038 | 品种配置以 `exchange + product` 为唯一粒度。具体合约、加权指数及其他同品种代码共用一条配置，例如 `AO2609` 与 `AOL9` 都复用 `SHFE.AO`；只要品种配置已存在，就不得再次联网获取或新增一条指数配置。`symbol_pattern` 用于快速匹配，但旧配置正则未覆盖指数代码时仍按代码提取出的 product 回退匹配。 |
| C-039 | 扫描器在已有 `exchange + product` 配置和带来源内置规则均未命中时，按通达信市场编号与品种代码联网查询公开期货交易参数表，结构化读取交易所、品种名、合约乘数和最小变动价位；交易时段从只读 TDX 实际时间边界生成。首个夜盘的前一交易日通过公开交易日序列查询，禁止自然日直接减一天。远程来源 URL、数据日期、核验日期和时段生成版本写入配置；联网、解析、交易所歧义或字段校验失败时仅将对应源文件保留为 `needs_mapping` 并显示具体原因。 |
| C-040 | C-023 中“参考实现端点选择”为权威的部分被本决策取代。笔只连接包含处理后的有效 K 线序列中的已确认顶底分型，且上涨笔必须满足“起点等于闭区间最低价、终点等于闭区间最高价”，下降笔必须满足“起点等于闭区间最高价、终点等于闭区间最低价”。相邻同类分型按后顶更高、后底更低淘汰旧端点；尚未形成有效分型的行情预览不作为未确认笔输出。线段结构端点由特征序列确定，不套用整段绝对最高最低断言。分型及笔/段端点额外保存极值来源原始 K 线位置。算法版本升为 6.0.0，禁止复用 5.x 缓存。 |
| C-041 | `docs/chanlun_108_algorithm_set_20260819` 的 `chanlun_108_strict` 规格取代旧参考包对三类点边界的严格不等口径。依据[第 20 课](https://chanlun108.cn/chanzhongshuochan108ke/20.html)“不跌破 ZG / 不升破 ZD”，第一次已完成回试满足 `B3 low >= ZG` 或 `S3 high <= ZD` 即确认，等号属于三类点；越过边界与未完成回试均不得发布。理论端点、确认时点和可用时点继续分离。缠论算法版本升为 7.0.0，消费三类点的策略版本升为 1.1.0，禁止复用旧缓存或策略定义。 |
| C-042 | `INV-ZS-004` 依据[第 18 课](https://chanlun108.cn/chanzhongshuochan108ke/18.html)与 `chanlun_108_strict` 的闭区间规格，取代 C-027 的标准线段中枢正宽限制。三个连续已完成同级构件计算 `ZD=max(lows)`、`ZG=min(highs)`，只要 `ZD <= ZG` 就发布标准中枢；`ZD == ZG` 作为点中枢保留，不与无交集混同，也不额外等待第四个构件。点中枢继续使用同一 `segment_zhongshu` 契约和图层，以零高度水平区域绘制。缠论算法版本升为 8.0.0，所有消费标准中枢或其派生信号的策略提升版本，禁止复用旧缓存。 |
| C-043 | `ALG-STR-002` 在本工程落为 `second_buy_only`：只消费 Python 已确认的 `signal_class=standard` 二买、线段、背驰和三买事件，不扫描原始 K 线重建私有结构。`strongest/normal/weakest` 三分支可独立启停并配置手数，且弱二买手数不得大于另外两类；弱二买的盘整背驰与内部完成前提由权威二买事件保证。入场后把与二买端点相接的首条已确认上升段作为唯一后继：不能严格突破前一上升段高点或在端点确认顶背驰时退出；与来源中枢关联或与二买同点的 B3 已确认且后继创新高、无背驰时发布 `handoff_to_B3_trend` 并继续持仓，后续标准卖点或二买来源修订退出。交易信号新增可选正整数 `quantity`；旧信号缺省为 1 手，单向净持仓平仓始终全平，手续费、滑点、保证金和盈亏均按实际手数计算。 |
| C-044 | `ALG-STR-003` 在本工程落为 `third_buy_only`，依据[第 20 课](https://chanlun108.cn/chanzhongshuochan108ke/20.html)、[第 79 课](https://chanlun108.cn/chanzhongshuochan108ke/79.html)和[第 107 课](https://chanlun108.cn/chanzhongshuochan108ke/107.html)只消费已确认标准 B3 及其来源线段中枢、回试段、后继段和背驰事件。固定级别为 `analysis_level=segment`；同一上涨中心链按已确认中心的 `(start_bar_index,end_bar_index,object_id)` 排序，只有相邻中心都向上离开且后中心 `DD >` 前中心 `GG` 时序号递增，否则从 1 重新计数。首中枢 B3 为高优先级，后续中枢 B3 可禁用或使用不高于首中枢的手数；确认 K 线成交量低于显式阈值时只发布风险过滤状态。B3 回试段前一上升段作为创新高基准，与 B3 端点相接的首条已确认上升段不能创新高或确认顶背驰时退出；创新高且无背驰后，出现新同级中枢不退出，而是继续持有到趋势顶背驰、标准 S3、已确认回拉重新进入来源中枢（含触及闭区间上边界 `ZG`）或 B3 来源修订。每个 B3 对象 ID 的第一次回试资格只消费一次，类三买、候选和重复修订不得重复入场。 |
| C-045 | `ALG-STR-004` 在本工程落为 `centre_oscillation_spread`，依据[第 24 课](https://chanlun108.cn/chanzhongshuochan108ke/24.html)、[第 33 课](https://chanlun108.cn/chanzhongshuochan108ke/33.html)、[第 38 课](https://chanlun108.cn/chanzhongshuochan108ke/38.html)、[第 39 课](https://chanlun108.cn/chanzhongshuochan108ke/39.html)、[第 45 课](https://chanlun108.cn/chanzhongshuochan108ke/45.html)、[第 49 课](https://chanlun108.cn/chanzhongshuochan108ke/49.html)和[第 92 课](https://chanlun108.cn/chanzhongshuochan108ke/92.html)实现固定线段级别的中枢震荡差价。这里的“次级别”严格映射为标准线段中枢内部已确认的组成线段，不引入第二个 K 线周期，也不实现旧参考方案中跨级别的 `centre_oscillation_swing`。信号层在同一活动中枢内比较相邻同向已确认线段的 MACD 同方向面积，后段力度收缩才发布关联该中枢的盘整背驰；策略不得重扫 K 线或自行计算背驰。底/顶背驰分别触发震荡买/卖，`Zn` 只决定强弱仓位和越界风险过滤，不能生成三类点。中枢核心宽度扣除显式估算往返成本后不足最小净空间、无快速执行能力时买点 `Zn < ZD`（卖点镜像为 `Zn > ZG`）、或达到单中枢进场次数上限时禁止新开仓。标准 B3/S3、中心离开、来源事实修订或九构件升级风险会停止本中枢震荡；由于当前阶段不运行跨级别组合器，独立策略先平掉震荡仓并发布 `handoff_to_trend` 图表事件，不在本实例中复制趋势持有算法。九构件分支只标记并按风险停止，不冒充已确认高级别中枢。缠论算法版本升为 9.0.0，受新增中枢内盘整背驰影响的策略同步提升版本，禁止复用旧缓存。 |
| C-046 | `ALG-STR-005` 在本工程落为 `same_level_decomposition_program`，依据[第 38 课](https://chanlun108.cn/chanzhongshuochan108ke/38.html)、[第 39 课](https://chanlun108.cn/chanzhongshuochan108ke/39.html)、[第 40 课](https://chanlun108.cn/chanzhongshuochan108ke/40.html)和[第 45 课](https://chanlun108.cn/chanzhongshuochan108ke/45.html)实现固定线段操作级别的同级分解机械程序。受 C-030 的单级别范围约束，规范分解单元固定为 Python 前一层发布的已确认、首尾连续且方向交替的 `segment` 事件，并在结果中显式记录 `decomposition_unit=confirmed_segment`；这是一套可替换的单级别投影，不宣称已经完成递归多级别走势划分。`odd_direction_is_down` 在运行前固定奇偶方向，禁止回看后改分组；同向 `Ai/Ai+2` 只比较方向结构端点，后段未严格创新极值（等号也算未创新）或其端点已有确认盘整背驰时执行反向操作。后段创新极值且无背驰时保持原方向，并在下一条反向确认单元 `Ai+3` 到来后，以其是否严格穿过 `Ai` 的方向极值分别发布“等待新同级结构”或“继续围绕原中枢”；两者都是当下分类，不预测后续。未确认单元不参与，已消费分解单元或背驰来源发生修订/删除时平仓并从修订时点重新起链，不用终态历史重分组。当前没有跨级别对象图时，标准中枢达到九构件只发布 `promote_level_candidate`，保守平仓并停用本小级别实例，不能冒充已确认高级别中枢或自动启动高级别策略。 |
| C-047 | `ALG-STR-007` 在本工程落为 `three_level_complete_classification`，依据[第 91 课](https://chanlun108.cn/chanzhongshuochan108ke/91.html)、[第 98 课](https://chanlun108.cn/chanzhongshuochan108ke/98.html)、[第 100 课](https://chanlun108.cn/chanzhongshuochan108ke/100.html)和[第 102 课](https://chanlun108.cn/chanzhongshuochan108ke/102.html)实现三层级完全分类。这里的级别不是三个 K 线周期，而是运行前固定且写入每个结果的显式对象图 `segment_center_chain_v1`：L0=`confirmed_segment_turn`（已确认线段背驰转折或标准一类点），L1=`confirmed_segment_zhongshu`（标准线段中枢、关联三类点及确认回归段），L2=`confirmed_center_migration_chain`（已确认同级中枢迁移链）。状态严格按 `WAIT_LOW_TURN → LOW_TURN_ACTIVE → MID_THIRD_POINT/MID_CENTER_CONTINUE → HIGH_CHANGE_CANDIDATE` 自低向高推进；中层向下离开时只接受低层底背驰/B1，向上离开时镜像接受顶背驰/S1。回归恰好触及 `ZD/ZG` 优先使用 C-041 的标准三类点分类，只有严格进入冻结核心才归为中枢延续。高层变化必须等待中层三类点完成，且迁移链必须从来源中枢终点开始；当前 L2 只能发布 `candidate`，不能冒充已确认高级别中枢。三种合法后续分支的处理能力均为显式运行参数，只要任一不可处理，`max_participation_quantity=0` 且不下单；已参与来源被修订/删除、新中层上下文取代旧上下文时，在当时已知 K 线上平仓、重置，不用未来事实回写历史。 |
| C-048 | `ALG-STR-008` 在本工程落为 `target_level_rebound_segmented_operation`，依据[第 47 课](https://chanlun108.cn/chanzhongshuochan108ke/47.html)、[第 56 课](https://chanlun108.cn/chanzhongshuochan108ke/56.html)、[第 98 课](https://chanlun108.cn/chanzhongshuochan108ke/98.html)和[第 107 课](https://chanlun108.cn/chanzhongshuochan108ke/107.html)实现目标级别反弹/回调的分段操作。固定对象图 `segment_rebound_rhythm_v1` 只由已确认标准 B1/S1 启动目标级别反弹/回调，明确排除盘整背驰产生的类一买/类一卖；操作级别固定为与转折端点相接的已确认 `segment`。第一条目标方向段完成后按显式手数部分兑现，第一条反向段完成且未严格穿过来源 B1/S1 极值时回补，等于来源极值不算破坏；首次形成的标准线段中枢确认后，必须等待关联该中枢的标准 B3/S3、完整回试/离开对象链及首条跟随段，只有严格突破离开段极值且端点没有已确认背驰时才发布 `trend_handoff`，绝不假设第二个同向段必创新高。相反三类点、反向段严格破坏来源极值、跟随段不创新高/背驰、来源对象修订或估算往返成本后无最小净空间时退出；多空完全镜像。交易信号保留 `close_long/close_short` 全平语义，并新增 `reduce_long/reduce_short` 按数量部分减仓、`add_long/add_short` 按数量同向加仓；执行模型使用 FIFO 批次分配入场成本并按实际减仓数量生成成交与交易记录。`side` 仍表示成交方向：买入类为 `long`，卖出类为 `short`。所有对象锚点、确认时点和可用时点继续分离。 |
| C-049 | `ALG-STR-009` 在本工程落为 `bottom_top_construction`，依据[第 82 课](https://chanlun108.cn/chanzhongshuochan108ke/82.html)、[第 88 课](https://chanlun108.cn/chanzhongshuochan108ke/88.html)、[第 99 课](https://chanlun108.cn/chanzhongshuochan108ke/99.html)和[第 108 课](https://chanlun108.cn/chanzhongshuochan108ke/108.html)分成不可混淆的精确层与粗略层。精确底部只由已确认标准 B1 启动，锁定 `start_bar_index` 等于 B1 端点的首个已确认标准线段中枢；该中枢最先确认 B3 时成功、最先确认 S3 时失败，顶部按 S1→首中枢→S3 成功/B3 失败完全镜像。成功仓位穿过底顶之间的连接走势继续持有，直到新的标准一类点移交下一构造；失败或来源 B1/S1、中枢、结果三类点修订时因果退出。粗略层使用已确认分型中间处理后 K 线的闭区间：底部分型区间下沿被严格跌破失败，连续收盘严格站上上沿成功，顶部分型完全镜像；等号既不成功也不失败。粗略层只发布观察状态和图表事件，绝不据此盲目下单。为使上层不回扫 K 线，`ChanFractal`/`fractals.parquet` 新增 `zone_low_i64/zone_high_i64`，缠论算法升为 10.0.0 并隔离旧缓存。 |
| C-050 | `ALG-STR-010` 需要同时消费高级别仓位信号和低级别 B1/S1，并分别维护核心仓与机动仓；在 C-030 的单级别范围内继续延期，不以同一 `analysis_level=segment` 信号流伪造跨级别程序。目录中下一项 `ALG-AUX-001` 落为 `aux_ma_kiss_legacy`，依据[第 11 课](https://chanlun108.cn/chanzhongshuochan108ke/11.html)、[第 12 课](https://chanlun108.cn/chanzhongshuochan108ke/12.html)、[第 14 课](https://chanlun108.cn/chanzhongshuochan108ke/14.html)和[第 72 课](https://chanlun108.cn/chanzhongshuochan108ke/72.html)发布 `aux_flying_kiss/aux_lip_kiss/aux_wet_kiss/aux_legacy_B1_candidate/aux_legacy_B2_candidate`。唇吻在短长均线进入接近区后同侧离开时确认；湿吻要求期间发生穿越并在离开接近区时确认，采样点直接从一侧跨到另一侧则当根确认；飞吻在接近区外由短均线走平后恢复原排列方向时确认。旧二买只标记同一多头排列的第一次同向吻；旧一买只在最近一次空头同向吻之后出现价格更低而负 MACD 柱绝对力度变弱时标记。所有结果都携带 `semantic_namespace=auxiliary`、`standard_signal=false`、`execution_allowed=false`；它们不创建或删除标准笔、线段、中枢、背驰和买卖点，也不产生 `trade_signal`。当前公共契约尚无 `auxiliary` 执行 kind，故只借用 `kind=strategy` 的因果事件/不可变 run 传输通道，目录名称、输出名、对象树和按钮必须明确“辅助、非标准、不交易”。 |
| C-051 | `ALG-AUX-002` 在本工程落为 `aux_macd_zero_axis_defense`，依据[第 24 课](https://chanlun108.cn/chanzhongshuochan108ke/24.html)、[第 25 课](https://chanlun108.cn/chanzhongshuochan108ke/25.html)、[第 50 课](https://chanlun108.cn/chanzhongshuochan108ke/50.html)与[第 103 课](https://chanlun108.cn/chanzhongshuochan108ke/103.html)实现简化风险开关。只有 DIFF 与 DEA 同时严格低于负零轴缓冲、连续达到配置根数时，才发布 `aux_macd_risk_off`，并记录 `max_participation_multiplier=0`；只有两线同时严格高于正缓冲、连续达到配置根数时，才发布 `aux_macd_risk_on_candidate`，解除本过滤器的参与限制但不创建仓位。等于边界或仅一条线越界都不计数。运行前固定的 `minimum_timeframe_minutes` 必须与数据集 K 线周期完全一致，禁止看完结果改周期；K 线周期仍不等于结构级别。事件明确为 `AUXILIARY` 风险层，`standard_signal=false`、`execution_allowed=false`、`opens_position=false`，不得称为标准 S1/B1，也不改变任何标准几何。当前同样只借用 `kind=strategy` 的因果事件/不可变 run 通道，正式结果不产生状态、阶段、交易、订单或成交。 |
| C-052 | `ALG-AUX-003` 在本工程落为 `aux_boll_bardo_warning`，依据[第 90 课](https://chanlun108.cn/chanzhongshuochan108ke/90.html)实现 BOLL 超强区、中阴候选、二类点区域和缩口预警。普通指标注册表新增唯一权威 `boll`，使用 `SMA ± k × population_stddev`；辅助算法不得复制指标。收盘严格高于上轨或低于下轨才进入超强区，等于轨道属于回到一般区；回轨后价格严格创新高/低且连续未有效重返同侧轨外才发布 `aux_boll_superstrong_exit`，它只表示转中阴候选。随后上轨首次确认向下转折发布二卖阻力区域，下轨首次确认向上转折发布二买支撑区域，均不得命名为标准 B2/S2。缩口预警只能存在于 `confirmed_trend_divergence_until_structural_resolution_v1` 上下文：由前一层已确认趋势背驰启动，在标准 B3/S3、中枢迁移、九构件升级或来源修订时结束；实现只组合已确认事件，不扫描 K 线重建结构，也不把该投影发布为标准 BARDO 对象。连续缩口只发布 `aux_boll_bardo_end_or_promotion_warning`，明确 `confirms_third_point=false`。四类事件均为 `AUXILIARY`、`standard_signal=false`、`execution_allowed=false`、`opens_position=false`，正式 run 保持零交易。 |
| C-053 | `ALG-AUX-004` 直接升级 `chan_engineering` 已有的 `center_monitor`，不另建中枢或复制几何。依据[第 92 课](https://chanlun108.cn/chanzhongshuochan108ke/92.html)，`A/B` 固定映射为标准线段中枢冻结核心 `ZD/ZG`，当前单级别投影下每个“次级震荡区间”固定映射为该中枢内已确认组成线段；`Z=(A+B)/2`、`Zn=(range_low+range_high)/2` 使用 `z_twice_i64/zn_twice_i64` 保存精确半 tick，旧 `z_i64/zn_i64` 只保留向下取整的兼容投影，比较与图表不得使用兼容投影。`Zn>Z` 为强、`Zn<Z` 为弱、等号中性，与组成线段方向无关。`Zn` 严格越过 B/A 分别发布 `cross_above_b/cross_below_a`；未越界时最近三个 Zn 严格抬高/降低才发布对应楔形预警，越界优先。前三个 Zn 统一在标准中枢确认时可知，后续每两条延伸构件统一在后一条确认时可知；每个中枢最多保留 9 点，不向较早端点回填。对象固定携带 `semantic_namespace=auxiliary`、`standard_signal=false`、`execution_allowed=false`、`confirms_third_point=false`。主图连接同一中枢的 Zn 曲线并绘制 Z 中轴，A/B 继续由标准中枢框表示；对象树显示具体预警及“不能确认三类点”。缠论算法升为 11.0.0，消费精确 Zn 的 `centre_oscillation_spread` 升为 1.1.0，旧缓存/run 不复用。 |
| C-054 | `ALG-AUX-005` 在本工程落为 `aux_daily_30m_classification`，依据[第 46 课](https://chanlun108.cn/chanzhongshuochan108ke/46.html)和[第 47 课](https://chanlun108.cn/chanzhongshuochan108ke/47.html)实现固定日盘经验分类。输入 profile 固定为 `Asia/Shanghai + trading_day + bar_end + 30m`，每个交易日必须依次且仅有 `10:00/10:30/11:00/11:30/13:30/14:00/14:30/15:00` 八根；缺根、夜盘、额外 K 线或时间模板变化均发布 profile 拒绝并且不保留当日分类，禁止从其他周期聚合。任意相邻三根 K 线的闭区间价格交集非空即为课程语境的“日内重叠区”；首个合格三根窗口冻结为第一重叠区，第二重叠区必须与第一重叠区价格严格不相交且两者之间至少存在一根不属于两区的单边 K 线。最终只在第八根收盘发布一重叠区、双重叠区或无重叠区及强弱子类，锚点使用交易日、时间和定点价格，不回填到更早 K 线。它使用独立 `semantic_namespace=heuristic` 和 `evidence_level=HEURISTIC`，固定 `standard_center=false`、`standard_signal=false`、`execution_allowed=false`、`opens_position=false`；不得写入标准中枢/买卖点或参与交易。当前复用通用 `chart_event` payload，无公共 Schema 形状变化；主图只投影日内重叠区矩形和最终分类标签，对象树显示经验解释。 |
| C-055 | `ALG-AUX-006` 在本工程落为 `aux_ma_sector_rotation`，依据[第 106 课](https://chanlun108.cn/chanzhongshuochan108ke/106.html)实现跨标的经验排名。默认八级均线梯子为 `5/13/21/34/55/89/144/233`，从显式反弹 episode 起按“第一条尚未攻克的均线”形成 1–9 级；等级不等同于缠论结构级别。运行必须提供至少两个 `1d + bar_end + trading_day + Asia/Shanghai` 数据集、活动 revision、统一复权模式/revision、episode 起点/可用时间及 `point_in_time` 成员区间，Go 从 catalog 解析所有 bars/meta 引用并经 PathGuard 限制在 `data_root`，Python 不扫描宇宙目录。板块平均只使用同一时间戳、当时已公开且有效的成员，达到显式覆盖率才发布。轮动只在标准 `divergence_kind=trend + top_divergence` 已确认后，从活动宇宙中筛选最近趋势底背驰已调整或等级落后的标的，并执行显式成交量容量过滤；来源背驰删除时因果删除候选。所有结果固定为 `heuristic/HEURISTIC`、`standard_signal=false`、`execution_allowed=false`、`candidate_only=true`，不生成订单或持仓。公共 `BacktestRequest`/`run.json` 新增不含路径的可选 `ranking_context`，内部载荷另含 Go 解析的 `ranking_datasets`；当前继续借用零交易 strategy run 通道。Vue 只显示当前标的的价格锚点，并在共享时间轴副图绘制全部板块平均等级，其他标的价格不得投影到当前价格轴。 |
| C-056 | `ALG-RISK-001` 在本工程落为独立目录项 `kind=risk_filter` 的 `unified_risk_execution_overlay`，位于策略决定与既有订单/成交执行器之间；它只能批准、减量或阻断策略手数，永不增加。公共 `BacktestRequest`/`StudyRequest` 增加可选 `risk_overlay` 以兼容旧调用，Vue 正式回测和训练—验证 Study 默认启用并完整提交已发布 AlgorithmRef、显式参数和点时 `RiskContext`。权重统一使用整数 ppm；默认禁止杠杆，开启时必须提供独立 approval id。覆盖层在订单拟执行 K 线使用当时已知的价格、成交量、组合状态和市场观察，拒绝早于信号可用时点的执行；未处理合法分支和开仓信号年龄只约束开/加仓，不得阻止已有仓位退出。持仓/板块/事件风险权重、单笔压力损失、成交量参与率和无杠杆上限依次取最小可执行手数；停牌及涨跌停按买卖方向阻断，开仓只在年龄内顺延，平/减仓持续顺延到回测终点。全平被流动性切成部分减仓时，当根执行获批手数并继续调度剩余退出；开/加仓被减量则不承诺补足。数据 revision 变化、陈旧/缺口超限、日内亏损或策略回撤达到阈值会触发不可逆的本 run 熔断；熔断后仍允许数据事实兼容且市场可执行的平/减仓。每次覆盖决定写入 `risk_decisions.parquet`、正式日志和 `object_type=risk_decision` 因果事件，图表/对象树展示批准、降仓、阻断和熔断，summary 保存四类计数；`risk_overlay` 参与 run/study 签名并写入不可变 manifest。 |
| C-057 | 108 算法集的完成度以 `docs/chanlun-algorithm-coverage.json` 为唯一逐项审计清单，不再以里程碑编号走到目录末尾作为完成判据。每个目录 `emits` 必须恰好归入已实现或缺失集合，并提供实现、测试、图表证据；`fixed_level_projection` 必须保留限制说明，不能升级为递归级别完成。源目录 SHA-256、27 个 ID 的顺序/唯一性、证据路径、状态规则和汇总由 Python 测试持续校验。10W 结论为 7 项完整实现、11 项固定级别投影、7 项部分实现、2 项延期，`catalog_complete=false`；后续严格从最早缺口 `ALG-GEO-001/002/003` 的在线生命周期开始。 |
| C-058 | `ALG-GEO-001/002/003` 的在线生命周期统一复用稳定语义 ID 做 revision upsert，不以屏幕位置或临时数组下标另建对象：处理后 K 线顺序、方向感知地合并并保留完整原始成员与高低点来源，初始方向为 `unknown` 且不得在未来方向形成后回填；分型在右侧处理后 K 线完成前为 candidate，之后以同一 ID 确认或失效；笔由已确认端点与当前分型候选派生 candidate，以同一 ID 确认或保留 invalidated 原因。笔在线状态公开为 `SEEK_FIRST_FRACTAL/UP_EXTENDING/TOP_FORMING/DOWN_EXTENDING/BOTTOM_FORMING`，完整转换历史保存在因果事件中，当前快照写入独立 Parquet。依据[第 82 课](https://chanlun108.cn/chanzhongshuochan108ke/82.html)，当前唯一数值明确的辅助强反转轮廓为顶部第三根严格跌破第一根低点且收盘不高于第一根中点，底部完全镜像；该标签固定为 `auxiliary`、`standard_signal=false`、`execution_allowed=false`。缠论算法升为 12.0.0，缓存 manifest 升为 2。 |
| C-059 | `ALG-GEO-005/006` 使用独立递归结构图，不把 K 线周期或旧 `analysis_level=segment` 当成结构级别。已确认标准线段中枢作为 L0；延伸到第九个已确认组成线段时，以第九组件的 `known_at_bar_index` 正式提升为已确认 L1，冻结来源 `ZD/ZG`，后续只以稳定 ID 修订为 extended/terminated。任意同级中枢的关系只比较完整振幅包络：`current.DD > previous.GG` 为上、`current.GG < previous.DD` 为下，等号及其余情况为重叠；单中心为盘整，连续同向严格脱离为趋势，其余链为高级别中枢候选并可导致走势重分类。已确认 L1 再进入相同分类器并可产生 L2 候选；候选不自动确认为中心，也不生成空层级。`level_center/level_movement` 使用独立 Parquet 与因果事件，旧 `movement_state` 和既有策略消费链在 10Z 前保持兼容且不得声称已迁移。缠论算法升为 13.0.0，缓存 manifest 升为 3。 |
| C-060 | `ALG-SIG-001` 的标准 B1/S1 使用候选—确认—失效生命周期。结构条件先于力度：至少两个同级中枢的完整 `DD/GG` 包络严格不重叠，最后同向离开段已完成并创新极值，再比较参考离开段与末段的同方向 MACD 面积，只有严格收缩才发布 candidate；MACD 不得独立创建一类点。候选端点锚定末段极值，后续首条方向相反的已确认低级别线段完成时，以相同 `signal_type + level_id +末段` 稳定 ID 发布 confirmed revision 并记录 `lower_level_turn_object_id`；结构修订使候选消失时保留 invalidated revision 和 `trend_structure_revised` 原因。L0 消费标准线段中枢，L1 只消费已确认九组件提升中枢，L2 候选中枢不产生信号。所有策略只允许显式已确认的一类点触发交易；生命周期对象进入 Parquet、Go API、Vue 回放、主图和对象树。缠论算法升为 14.0.0，缓存 manifest 仍为 3。 |
| C-061 | `ALG-STR-005 promote_level` 只由权威结构图的已确认 `level_center` 触发。九组件兼容 `segment_zhongshu` 仍可先发布 `promote_level_candidate`，但候选不得平仓、停用当前实例或满足目录正式输出；确认事件必须满足 `confirmed=true`、父级等于当前操作级别、`promoted_from_center_id` 指向当前已确认且至少九组件的来源中心。确认后策略平掉小级别仓位、清空旧序列、将活动结构级别从 L0 迁移到 L1 并进入等待高级别同级序列状态；来源删除或降为未确认时按当时已知时点重置。该过程只使用同一数据集结构级别，不引入或映射第二个 K 线周期。策略版本升为 1.1.0。按用户范围，`ALG-STR-006` 与 `ALG-STR-010` 明确延期，其余固定级别投影保持现有测试、回放与图表能力。 |
| C-062 | 里程碑 11A 的默认研究全集由算法目录元数据决定，不在 Vue 或 Go 硬编码：仅选择 `comparison_eligible=true` 且 `research_role=formal_strategy` 的 13 个已发布策略，排除 MA 示例、辅助算法、风险过滤器以及延期的 `ALG-STR-006/010`。每批 comparison 固定单一 dataset/revision、完整参数、区间、资金、成本、成交、统一风险覆盖、随机种子和最少交易数；Python 顺序执行各策略并在批次内共享一次已完成的权威缠论因果结果，成功策略仍分别生成不可覆盖的正式 run。单策略失败只记入该批结果，不阻断其余策略；Go 负责任务编排、校验、取消和成功子 run 注册，comparison 以临时目录、同目录原子重命名和最后写 `_SUCCESS` 提交到 `comparisons/{comparison_id}`。11A 只交付一键执行、基础结果和进度；排行榜/Pareto、历史研究恢复增强、详情叠加及行情条件归因分别留给 11B、11C、11D。 |
| C-063 | 里程碑 11A-H–11D 使用版本 2 comparison 事实：Go 对数据、完整有序策略引用与规范参数、区间、执行、资金、风险、种子、最少交易数、Python 引擎和聚合器版本生成稳定总签名；Python 把批次缠论运行时提交为内容寻址只读依赖，Go 校验 PathGuard、`_SUCCESS` 和内容哈希；运行 journal 保存完成项与剩余项。Python 是互斥分层、收益—回撤 Pareto 和交易条件归因的唯一权威。交易直接保存入/出场信号与订单关联，只消费 `entry_signal_known_at_bar_index` 之前可见的结构事件；未知状态进入 `unknown`，条件桶只汇总已平仓 realized PnL 并保留对账总额。旧 run 继续可读，缺少新列时标为不支持归因；Vue 只排序、筛选、归一化绘图和投影既有事实。 |
| C-064 | 里程碑 12A 将权威历史导入范围与 `chart.begin_dt/end_dt` 完全解耦：图表默认窗口只影响首次展示，不得裁剪 normalized Parquet 或参与 data revision。每个新修订保存精确 `trading_day_count` 和 `independence_group`；独立组默认使用 `exchange.product`，允许导入请求显式覆盖，覆盖值参与 revision。AO 具体合约和加权指数均归入 `SHFE.AO`。研究认证的数据门禁为每组至少 504 个交易日且至少 3 个合格独立组；不足时允许浏览和回测，但只标记 exploratory。回测权益逐行保存权威 `trading_day`，另存每日收盘收益；年化收益、波动率和 Sharpe 只由日收益按 252 日口径计算，样本不足或方差为零时返回 null 和稳定 reason code，禁止对分钟收益直接套用 `sqrt(252)`。旧 dataset/run 保持可读，但缺少新事实时不得获得认证资格。 |
| C-065 | 里程碑 12B 的 Research Study 固定一个已发布正式策略版本、规范化参数、执行/成本、资金和随机种子，在每个同周期 `dataset_id + data_revision` 上分别运行，禁止拼接合约。研究签名包含有序数据版本、独立组、各自范围、策略与参数及引擎/聚合器版本；每个成功项仍是不可变正式 run。跨数据集日收益先按交易日在同一 `independence_group` 内等权，再在可用独立组间等权，因此 AO 具体合约和连续合约只贡献一组证据；504 日门槛按本次研究实际产生的组内交易日并集计算，不得用数据集全量覆盖冒充所选研究区间。运行 journal 保存 payload 和已完成数据集，Go 进程重启后可沿用同一 `research_study_id`，但每次恢复使用新的 Python 内部 job id；最终以同目录临时写、原子重命名和最后 `_SUCCESS` 提交到 `research-studies/{id}`，完成目录拒绝覆盖。12B 的 `data_status` 只表达 12A 数据门禁，不提前授予 12C–12E 的研究候选或可靠候选认证。 |
| C-066 | 里程碑 12C 在同一 Research Study 契约上增加可选 `walk_forward`，保持不带该字段的 12B 固定参数请求兼容。默认滚动窗为 252 个训练交易日、随后 63 个验证交易日、步长 63 日；拒绝小于验证窗的步长，避免重复计算同一日期的样本外证据。折边界只由 Parquet 的权威 `trading_day + bar_index` 生成；每个 train/validation run 的 `warmup_from_bar_index` 固定为研究数据起点，策略和缠论顺序推进到折终点，而订单、权益与指标只在折区间产生并计分。每折先完成全部训练候选并仅按训练目标与硬约束排名；没有可行候选则记录稳定失败码，验证结果永不参与选参。样本外日收益按同组数据集等权、再按独立组等权，保存为 `out_of_sample_daily_returns.parquet`；盈利折比例和参数稳定度也先在同一独立组内平均、再在组间等权，避免相关合约重复加权。所有训练候选和验证 run 均为不可变正式子 run；研究聚合器版本升为 2.0.0。12C 不执行成本场景，也不据此提前授予 12D–12E 的可靠候选认证。 |
| C-067 | 里程碑 12D 的压力套件版本固定为 `1.0.0`，且只允许依附走步研究：每折直接冻结训练窗已选参数，在同一验证窗分别执行 `baseline`、`cost_1_5x`、`cost_2x`、`extra_slippage_1_tick`、`extra_slippage_2_ticks`、`delay_1_bar` 和 `volume_participation_10pct`，压力场景禁止重新调参。成本倍数同时乘佣金和基础滑点，额外 tick 在此后叠加；延迟在默认成交时点之后增加整根 K 线。成交量上限采用 `volume_cap_ioc`：最大成交手数为 `floor(bar_volume * participation_rate)`，允许部分成交并立即撤销余量，零成交量产生零成交而非假设完全成交。订单保存请求量和实际成交量，summary 保存成交率；每个压力子 run 的 execution 完整记录场景、成本、滑点、延迟、参与率和成交模式。跨数据集场景收益继续先组内、后组间等权；首个失效场景按固定套件顺序定义为首个无可用 run 或等权净收益不为正的场景。研究聚合器版本升为 3.0.0；12D 只提供压力证据，不提前执行 12E 的统计认证。 |
| C-068 | 里程碑 12E 的认证只使用独立组等权的样本外日收益和样本外交易事实，训练收益不得进入认证。统计套件 `1.0.0` 默认使用固定种子的 5 日非循环移动区块 bootstrap 2,000 次，输出总收益、日均收益/期望值和最大回撤的 95% 置信区间；全部尝试参数组合必须披露，实际入选候选之间的显著性以 Holm 方法校正并提示多重比较风险。数值参数邻域只采用请求中已声明候选序列的紧邻档位，每折冻结其余参数，在原验证窗执行正式不可变 neighbor run；核心门禁通过率先组内、再组间等权，至少 60% 才稳定。同组相关合约在交易数、折数、交易日和期望值门禁中也只贡献一组证据。探索性、研究候选和可靠候选由 Python 权威判定；可靠候选严格要求至少 504 个交易日、3 个独立组、200 笔样本外交易、盈利折比例不低于 70%、2 倍成本及延迟一根后仍为正、日均收益 bootstrap 95% 下界大于 0、最大回撤不超过 20% 和邻域通过率不低于 60%。所有级别都必须显示“仅为历史研究证据，不保证未来盈利”。研究聚合器升为 4.0.0。真实 YL9/AOL9 验收只有两个独立组，必须保持探索性。 |
| C-069 | Research Study 接受 1–32 个同周期数据集。单数据集模式用于“一标的一策略”的走步、成本压力、bootstrap 和参数邻域探索，仍生成完整不可变 Study 与子 run；它不伪造跨标的独立性，`eligible_independence_group_count < 3` 必然阻断可靠候选。两个及以上数据集继续按组内、组间两级等权。Vue 在只选一个数据集时允许提交并明确显示“只能形成探索性证据”，没有提交任务时显示“尚未运行”而不是易误解的 `idle · 0%`。 |

## B. 本规范为首版选定的实现方式

| ID | 选择 | 原因 |
|---|---|---|
| A-001 | 原始 TXT 只读，标准化为 Parquet | 兼顾压缩、类型、Go/Python互读和范围访问。 |
| A-002 | Vue 仅访问 Go；Go 为唯一公共 API | 避免浏览器绑定 Python 与磁盘细节。 |
| A-003 | Go 与 Python 使用本机 HTTP/JSON 控制面 | 首期易调试；大数据通过共享文件，不受 JSON 性能限制。 |
| A-004 | 长任务采用创建任务加轮询 | 首期不引入 WebSocket。 |
| A-005 | 图表使用 Lightweight Charts 5.x 的单图多 pane 能力和 custom primitives | 保留 TradingView 风格的时间轴、坐标与扩展能力。 |
| A-006 | 默认首次加载 3000 根、每次向左预取 1500 根 | 位于此前建议的 2000–3000 和 1000–2000 范围中。 |
| A-007 | 距左边界不足一个当前屏幕宽度时预取 | 降低用户看到空白的概率。 |
| A-008 | 缩放防抖 150 ms；同一数据集只允许一个历史预取任务 | 避免重复请求。 |
| A-009 | 布局按权重保存；绘图按时间和价格保存 | 屏幕尺寸变化后可正确恢复。 |
| A-010 | 回放与回测共享同一 Python 因果引擎 | 防止信号不一致和未来函数。 |

## C. 为后期预留但首版不得实现

- 实时行情适配器、WebSocket 增量推送。
- 实盘订单、持仓和交易柜台。
- 多用户认证、权限、云端工作区。
- 分布式任务队列。
- 数据库索引与对象存储。
- AI 自动选择指标、策略和优化目标。

预留不代表提前增加依赖。首期只需保持接口和模块边界可扩展。

## D. 待项目骨架生成时锁定的非业务版本

- Vue 3 的具体补丁版本。
- Vite、TypeScript、Lightweight Charts 的具体补丁版本。
- Go Parquet 库与 Python Parquet 库的具体版本。
- Python Web 框架的具体版本。

Codex 应从生成当日可兼容 Go 1.25.7、Python 3.14 的稳定版本中选择，写入锁文件，并在 README 记录；不得在代码中使用浮动 latest。

## E. 待确认实现选择

以下选择不改变业务语义，保持最小、可替换且已有测试覆盖：

| ID | 当前选择 | 可替换边界 |
|---|---|---|
| P-001 | 里程碑 0 的 Python 内部服务使用标准库 `ThreadingHTTPServer`，任务只保存在进程内并停留于占位状态。 | 里程碑 3 引入真实任务执行时可在 `tvbt.api` 内替换，不改变 `/internal/v1`。 |
| P-002 | Go 使用 `yaml.v3` 严格加载配置，使用 `lumberjack` 承担 50 MiB、9 个 gzip 备份的轮转。 | 保持配置字段和固定文本日志语义即可替换实现。 |
| P-003 | Python 配置加载器只解析当前 `app.yaml` 所需的 YAML 映射和标量子集，以保持运行时零第三方依赖。 | 若后续配置需要 YAML 列表或锚点，可引入已锁定解析器，不改变配置 Schema。 |
| P-004 | 前端源码位置由 Vite 预转换插件注入，日志队列上限为 1000 条，满时优先丢弃 TRACE/DEBUG。 | 队列上限可配置化；真实 `.vue`/`.ts` 文件和行号不可退化。 |
| P-005 | 相对 `storage.data_root` 按服务启动工作目录解析；仓库脚本和 VS Code 调试配置始终以仓库根目录启动服务。 | 后续可显式增加 `base_directory`，但持久化引用仍须是 `data_root` 下正斜杠相对路径。 |
| P-006 | 里程碑 1 的缺口仅在同一交易时段内按周期检测并标记，不合成 K 线；尾部交易日若最后一根未达到最后一个配置时段的预期结束位置，则标记为不完整。`bar_start` 的预期尾部位置为时段结束减去一个周期，`bar_end` 为时段结束。 | 后续若引入更精细的交易所临时休市规则，可扩展交易日历或时段配置；不得改变“不自动补 K 线”的语义。 |
| P-007 | Go 的 K 线热缓存最多保留 8 个已解码数据集修订，按最近使用淘汰；每次查询仍先从 catalog 验证活动 revision 与 `_SUCCESS`。 | 上限后续可配置或改为按字节预算，公共范围查询语义与 revision 校验不变。 |
| P-008 | Vue 首次从 Go 缓存尾部 3000 根，但初始可视窗口约为最新 300 根；进入左缓存边界一个屏幕内时合并为单个 1500 根预取请求。 | 初始可视根数可在布局配置中替换；3000/1500、generation_id 和单飞规则不变。 |
| P-009 | 普通指标由 Python 以 PyArrow 25.0.0 读取标准化 Parquet，并用纯 Python `float64` 语义实现 MA、MACD、ATR；Go 按 Python 发布的参数 Schema 补齐默认值并生成规范缓存键。MACD 使用首值初始化 EMA，ATR 使用 Wilder 平滑。 | 指标实现和 Parquet 库可在保持算法版本、source_hash、黄金值和缓存目录契约的前提下替换；Vue 不得复制这些算法。 |
| P-010 | 工作区布局与各数据集绘图分文件保存，使用各自单调 revision 和 `If-Match` 乐观并发；绘图交互态留在 Vue，持久化只保存时间/定点价格锚点。 | 后续可增加工作区级事务清单，但不得引入像素锚点、覆盖并发修改，或改变布局和绘图文件的原子提交语义。 |
| P-011 | 里程碑 5 的缠论语义以 `docs/structures.py`、`docs/multilevel.py`、`docs/engineering.py` 为参考：包含关系按已建立方向合并；分型使用左右各 2 根严格极值并等待额外独立 K 线封存；笔端点按同类更极端替换、原始 K 线跨度和 ATR 门槛确认；中枢由三笔严格重叠建立并记录 confirmed/extended/left 与离开方向。按 C-015 不生成线段。 | 参数与实现可在提升算法版本并保留前缀不变性、稳定 ID、`known_at_bar_index` 和因果事件契约时替换；不得把完整历史终态倒灌到更早时点。 |
| P-012 | 里程碑 6 将完整因果事件预计算到 `cache/replay/<cache_key>/events.parquet`；缠论即使回放区间位于历史中段，也从数据集起点累计状态到 `to_bar_index`，避免固定预热破坏严格一致性。Vue 只缓存一次事件并用递增事件索引推进；后退/跳转时从事件索引重建。当前回放请求和游标按数据集 revision 自动保存到浏览器本地存储。 | 后续可增加稀疏 UI 快照优化远距离后退，或将策略算法加入同一事件生成入口；不得改变 cache_key 事实集、事件顺序或游标移动不调用 Python 的规则。 |
| P-013 | 里程碑 7 的示例策略为确定性的 MA20 跌破—反抽—失败做空状态机；回放和回测共同调用 `run_strategy`。执行模型使用单向净持仓、收盘信号、默认下一根开盘成交；支持多空开平、固定/比例手续费、tick/bps 滑点、合约乘数和保证金。样例策略不发止损/止盈单，因此同根冲突规则只记录在 run 事实中而不触发。 | 可增加新的 Strategy 实现和订单类型，但必须继续通过统一 Transition、稳定 reason_code、相同信号事件及不可变正式 run；不得让策略直接创建成交。 |
| P-014 | 里程碑 8 将 Go 任务摘要作为独立 JSON 持久化在 `tasks/jobs`；重启时仅把无法继续执行的活动任务转为 `interrupted`，已完成和失败任务保持终态。过期临时目录与显式清理的正式缓存都使用同卷原子重命名移入 `trash`，不在应用路径中递归删除。缓存工具默认 dry-run，且只接受固定缓存种类、64 位小写十六进制键、普通目录和 `_SUCCESS`。 | 后续可增加 trash 保留策略或任务列表分页，但不得把删除范围扩大到未经 PathGuard 和名称白名单验证的路径，也不得把重启后的工作误报为继续运行。 |
| P-015 | 里程碑 9 的优化器是独立 Python 模块，只能构造并调用标准 BacktestRequest；grid 按声明顺序展开，seeded random 用显式种子对同一有限笛卡尔积无放回抽样，预算上限 100、组合上限 100,000。硬约束只用于训练集筛选，验证集不参与选参；训练区间必须严格早于验证区间。每个 Evaluation 生成训练/验证两个不可变正式 run，Study 另存排名、主目标差距与排名相关性。 | 可在保持 Study/Evaluation/Backtest 契约、时间外验证和可复现性的前提下增加 walk-forward、成本敏感性、贝叶斯或 AI 调用方；不得让优化器读取 UI 当前窗口、修改策略源码、复用验证集选择参数或绕过正式回测入口。 |
| P-016 | 一键启动默认固定使用 `D:\ProgramData\anaconda3\envs\pydev3.14\python.exe`，以只读方式准备 `AOL9` 加权连续合约样例，并以原子合并补充该样例缺失的品种映射与交易日历条目；随后启动三端、扫描/导入并由 Vue 优先选择 `SHFE.AOL9.5m`；`TVBT_PYTHON` 只作为显式本机覆盖入口。 | 后续可改为安装器或可视化启动器，但仍须检查强制版本、保持原始历史文件只读、保留已有运行配置条目、限制数据目录边界，并按精确进程树停止服务。 |
| P-017 | VS Code 的全栈调试项先统一检查强制版本及 5173/8080/8091 端口，全部空闲后才以无覆盖方式准备 AOL9 样例并同时启动三端；端口冲突会在启动任何调试子进程前列出服务名和 PID。Vite 只轮询 Go 公共接口，确认 Go/Python 均为 `ok` 后检查 catalog；缺少 `SHFE.AOL9.5m` 时通过 Go 扫描并仅导入该样例。AOL9 数据集就绪后输出标记，再以 `openExternally` 请求系统默认浏览器打开图表页。 | 可替换浏览器启动器或就绪提示形式，但不得允许端口冲突后留下半套服务，不得覆盖历史文件或已有数据集、在 Go/Python 或 AOL9 数据集未就绪时提前打开页面，也不得让浏览器直接探测 Python。 |
| P-018 | 国内期货图表使用涨红跌蓝绿：阳线和对应成交量画成红色空心轮廓，阴线和对应成交量画成蓝绿色实心；十字星按 `close >= open` 归入涨色。新工作区及没有已保存指标源的旧工作区默认通过 Go 创建 Python MA20、MA60、MACD(12,26,9) 全历史计算，Vue 只按范围绘制缓存结果；MACD 红绿柱使用类似 K 线上下影线的 1 CSS 像素细竖线。 | 配色、线宽及默认指标集合可进一步做成用户主题/模板；不得把 MA/MACD 算法复制到 Vue，已保存且包含指标源的工作区须按原配置恢复。 |
| P-019 | 缠论上层结构按首次变化笔位置增量更新：线段扫描器保存每个笔前缀的小状态，笔尾修订时回滚到共同前缀；已离开中枢作为稳定前缀复用；只有已确认线段变化才重算段中枢、走势状态、背驰和买卖点；已确认段的 MACD 面积按实际端点缓存。检查点恢复时允许从现有笔一次性重建这些非持久运行索引。 | 可替换内部状态表示或增加稀疏快照，但必须继续通过增量与强制全量重扫的逐事件等价测试，不得改变对象 ID、修订号、`known_at_bar_index`、事件顺序或最终对象。 |
| P-020 | 界面动态修改的 `StrategySource` 总可见性和九类图层可见性统一保存到独立的 `data_root/workspaces/{profile_id}/strategy-source-config.json`；记录按 `dataset_id + data_revision + source_id` 区分。Vue 采用 300 ms 防抖，通过专用 GET/PUT API 自动持久化；Go 原子写入并使用独立单调 revision。布局中的同名字段仅作为旧文件兼容回退，动态配置存在时必须优先。 | 防抖时长和文件名可替换；动态显示配置不得重新混入布局保存链路，不得绕过 Schema、原子写入和 revision 冲突检测，也不得把只读缠论语义对象复制为普通绘图。 |
| P-021 | `docs/chanlun_bi_108_testcases_20260817` 的 `chanlun_108_strict` 数值夹具作为笔流水线的持续回归输入，由项目适配测试直接调用当前 `ChanEngine`。其中关于分型、包含、成笔、同类取舍、精度、端点修订和笔/段边界的案例约束笔实现；`L83_BI_CANNOT_BUILD_CENTER` 属于中枢层级定义，不在“验证笔算法”任务中覆盖 C-026/C-027 已确认的笔中枢与标准线段中枢双模型。 | 若要取消笔中枢或把第 91 课四状态新增为公共语义对象，必须作为中枢/契约里程碑单独修改算法版本、Schema、缓存和三端实现，不能在笔夹具适配中隐式改变。 |
| P-022 | 第 108 课“有效站住”当前实现为分型确认后的 K 线连续 `coarse_effective_hold_bars` 根收盘严格越过分型区间另一侧，默认 1 根；确认分型的同一根 K 线不参与计数。区间内、边界等号或中断会把连续计数清零，粗略分支始终不交易。 | 有更明确的课程口径时可替换连续确认根数或有效性过滤器，但必须作为显式运行参数留档、保持严格边界与前缀不变性，并且不得把粗略分型区间升级为自动交易信号。 |
| P-023 | 第 11、12、14 课中的“靠近”“略略走平”和“背驰式下跌”没有给出统一数值阈值。`aux_ma_kiss_legacy` 当前将接近区定义为 `abs(short_ma-long_ma) <= proximity_ticks*tick_size_i64`（默认 1 tick），将走平定义为 `abs(short_ma[t]-short_ma[t-1]) <= flat_slope_ticks*tick_size_i64`（默认 1 tick）；边界等号属于接近/走平。旧一买代理使用与普通指标注册表完全相同的 MACD(12,26,9)：最近空头吻结束后先记录首个更低低点及负柱，再在至少 `legacy_divergence_min_bars`（默认 1）根后出现更低低点且柱值更接近零时确认候选。短/长均线默认 5/10，全部参数在运行前固定并写入 run；关闭 `enable_legacy_b1_macd_proxy` 时不发布旧一买候选。 | 阈值、周期和力度代理可在课程口径或样本外验证支持下替换，但必须提升算法版本、继续显式参数化、通过等号和前缀测试；不得把辅助候选改名为标准 B1/B2 或让它直接下单。 |
| P-024 | 第 103 课没有量化“陷入”和“重新站住”的确认根数。`aux_macd_zero_axis_defense` 默认使用普通指标注册表的 MACD(12,26,9)、`zero_axis_buffer_ticks=0`、`risk_off_confirm_bars=1`、`reclaim_confirm_bars=2`；缓冲以 `tick_size_i64` 转为 MACD 价格单位，不使用浮点 epsilon。算法定义保留课程示例和算法包配置的 60 分钟默认最低周期；Vue 在用户选择当前数据集运行时把该显式参数预填为数据集实际分钟数，Python 随后要求二者完全相等，因此 AOL9 5 分钟验收记录为 5，而不是静默把 5 分钟冒充 60 分钟。支持 `Nm/Nh/Nd` 数据集周期并规范化为分钟。 | 周期、缓冲与确认根数可按执行能力和样本外验证替换，但都必须在运行前固定并写入 run；改变默认状态机口径时提升算法版本。未来组合风险层可消费参与倍数，本里程碑的独立展示适配器不得自行平仓、开仓或恢复策略仓位。 |
| P-025 | 第 90 课没有量化“有效回到超强区”“轨道转向”和“收口”。`aux_boll_bardo_warning` 默认使用 BOLL(20, 2 个总体标准差)、`effective_reentry_bars=2`、`failed_reentry_confirm_bars=2`、`band_turn_confirm_bars=1`、`band_turn_min_change_ticks=0`、`contraction_confirm_bars=3`、`contraction_min_width_drop_ticks=0`；变化量必须严格大于 tick 阈值，等号会中断确认。`boll_stddev_milli=2000` 避免跨进程十进制默认值漂移。算法定义保留课程示例的 30 分钟观察周期，Vue 为当前数据集预填实际分钟数，Python 强校验完全相等；`level_mapping_profile_id=1` 明确采用当前数据集 BOLL + 当前标准线段层结构事实，并不声称 K 线周期等于结构级别或固定对应另一个低周期。 | BOLL 公式口径、周期映射和连续确认阈值可在图形回归或样本外验证支持下替换，但必须显式写入 run、提升算法版本并继续通过等号、未完成、事件时点和前缀不变性测试。未来若发布独立 BARDO 公共对象，可替换当前事件投影；不得让 BOLL 自行确认标准一二三类点。 |
| P-026 | 第 92 课没有量化“缓慢提高/降低”。首版沿用既有监视器的最小三个点窗口，但改为双倍定点整数上的严格单调，不使用浮点 epsilon 或最小斜率；最新 Zn 等于 B/A 仍是“未突破”，可形成楔形，只有严格越界才优先标记跨界。当前 C-030 单级别对象图没有独立的次级别趋势及其末中枢对象，因此不从 K 线私建“趋势型次级走势且末中枢在外”分支，也不输出原文未给公式的下一震荡区间预测。 | 若未来提供递归级别对象图，可在消费已确认下层走势/中枢的前提下增加该分支；窗口、斜率或预测公式只有在显式参数化并经样本外/图形回归验证后才能替换，必须升级算法版本且不能改变第三点仍由结构模块确认的边界。 |
| P-027 | 第 46 课没有量化收盘“接近全天高/低点”，也没有规定第一组三根同时包含全天高点和低点、以及无重叠区非单边走势的程序判定。首版不发明接近度 epsilon，而是输出收盘相对全天极值和冻结重叠区的精确位置；第一组三根同时含全天高低时显式记为 `dual_extreme_balance`，不任意套用强/弱优先级；无重叠区的 `up/down/flat` 仅按首根开盘与末根收盘比较，并保持经验候选语义。第一合格窗口和第二合格窗口的选择固定为 C-054 的因果扫描顺序。 | 若课程权威补充、经标注图形回归验证的数值口径或可替换会话模板出现，可增加显式 profile/参数并升级算法版本；不得事后按结果选择阈值，也不得把经验重叠区改名为标准中枢或据此直接下单。 |
| P-028 | 第 106 课没有给出程序化“攻克均线”、板块缺失覆盖率、轮动候选优先级或容量阈值。首版把攻克固定为 episode 起点以后复权收盘至少一次严格高于该 MA，等号不算；只有最大周期完成预热才发布等级。板块均值以 `class_sum/count` 为权威事实，千分位均值使用确定性四舍五入；默认覆盖率 800‰。候选排序依次为最近趋势底背驰已调整、板块均值高、当前等级高、dataset_id，默认使用 20 根平均成交量和最多 20 个候选，所有阈值均在运行前固定并写入 run。 | 可在权威补充或样本外验证后替换攻克确认、覆盖、容量和排序规则；改变默认语义必须提升算法版本并继续通过等号、点时成员、来源删除和前缀测试，不得用事后成分股、未复权价格或收益结果回选候选。 |
| P-029 | 算法目录没有量化统一风险层的权重、压力损失、陈旧数据、信号有效期和事件风险阈值。首版全部显式参数化：默认持仓/板块上限 100,000/300,000 ppm，单笔压力损失 10,000 ppm、每手压力损失 100,000 money-i64，日内亏损 20,000 ppm、策略回撤 150,000 ppm、成交量参与率 100,000 ppm，陈旧/缺口容忍均为 0 根，开仓信号年龄 3 根，事件风险持仓上限 50,000 ppm；触及亏损/回撤阈值即熔断。没有额外 observation 时只表示当前数据 revision、正常交易、零陈旧/缺口和无活动事件风险，不推断外部市场事实。当前执行器是单标的、单向净持仓，因此板块上限只对本标的所属板块暴露取更小上限，不虚构跨标的相关性、对冲或净额；压力损失按显式每手 money-i64 计算。 | 所有默认阈值、压力模型、市场 observation 供应器和组合聚合器均可按交易品种与样本外验证替换，但必须在运行前固定、进入签名/manifest、提升语义版本并保持“只降不增、默认无杠杆、点时可用、无保本/保收益承诺”。未来多标的组合层必须消费真实持仓账本和 sector 映射，不能把当前单标的限制包装成组合风控。 |
| P-030 | 第 82 课还描述第二根长上影线或长阴线可增强顶分型力度，但未给出统一数值阈值；10X 不发明影线/实体比例，未命中 C-058 明确轮廓的分型统一标为 `unclassified`。 | 若权威补充或经标注图形回归确认阈值，可新增显式 strength profile 并提升算法版本；不得事后按收益选择阈值，也不得让辅助强弱直接交易。 |
