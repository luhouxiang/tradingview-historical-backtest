# AGENTS.md

本文件适用于仓库根目录及全部子目录。若子目录存在更具体的 AGENTS.md，以更具体文件为准，但不得违反本文件的架构边界和数据正确性规则。

## 1. Codex 的工作方式

- 开始任何实现前，先阅读 README.md、DECISIONS.md 和当前里程碑引用的文档。
- 先列计划，再改代码；每次只实现 docs/09-implementation-roadmap.md 中一个明确里程碑。
- 不得用“先做一个简化版本”为由改变已确认的数据语义、未来函数规则、文件边界或进程职责。
- 遇到未定义的小细节，采用最小、可替换、可测试的实现并记录到 DECISIONS.md 的“待确认实现选择”；遇到会改变数据语义或用户交互的歧义，先询问用户。
- 保持 Go、Python、Vue 3 三端独立。不得把 Python 算法复制到 Go 或 Vue，也不得让 Vue 直接读取磁盘文件。

## 2. 强制版本

- Go：go1.25.7。
- Python：3.14。
- 本仓库的 Python 命令、测试、类型检查和格式检查必须直接调用
  `D:\ProgramData\anaconda3\envs\pydev3.14\python.exe`（项目 Codex 配置中的
  `TVBT_PYTHON`）；不得使用 PATH 中的其他 `python` 或 `py`。
- Vue：Vue 3 + Composition API + TypeScript。
- 前端依赖使用包管理器锁文件；Python 使用锁文件；Go 提交 go.mod 和 go.sum。
- CI 和本地版本必须一致。版本不满足时应明确失败，不得静默降级。

## 3. 不可违反的架构边界

### Vue 3

只负责：

- UI、图表渲染、坐标投影、命中检测和交互。
- 多窗格尺寸管理、对象树、用户绘图、回放控制。
- 调用 Go 公共 API。
- 将浏览器日志批量上传给 Go。

禁止：

- 直接访问历史目录、Parquet 或 Python。
- 在浏览器中重新实现权威指标、缠论或策略逻辑。
- 因缩放、改变窗格高度或拖动右侧面板而调用 Python。

### Go

只负责：

- 扫描原始历史目录、识别数据源、导入和质量检查。
- 维护 catalog、读取 Parquet、按范围向 Vue 返回 K 线和结果。
- 公共 API、任务生命周期、并发控制、取消、超时和错误映射。
- 调用 Python 内部 API，并验证所有文件引用位于 data_root 下。
- 保存工作区布局、绘图和浏览器日志。

禁止：

- 实现第二套策略或缠论算法。
- 通过 JSON 把数万根 K 线复制给 Python。
- 修改原始历史文件。

### Python

只负责：

- 普通指标、缠论、策略状态机、因果事件流。
- 回放与回测的统一逐 K 线引擎。
- 订单、成交、持仓、权益、统计和参数评估。
- 读取 Go 指定的标准化 Parquet，写入 cache 或 runs。

禁止：

- 直接服务浏览器。
- 自行扫描任意磁盘目录。
- 把最终全历史结果倒灌到更早的回放时点。

## 4. 数据正确性红线

- 原始 TXT 永不原地修改。
- 数据集身份必须包含 dataset_id 和 data_revision。
- 时间字段必须同时保留 trading_day、timestamp_utc、source_hhmm 和 bar_index。
- 夜盘自然日期必须通过交易日历映射，禁止简单减一天。
- 缺失 K 线不自动补齐；零成交量 K 线不删除。
- 价格在持久化、订单和成交中使用定点整数；指标内部允许 float64。
- 缠论对象以时间和价格锚定，不能只保存屏幕像素或当前数组下标。
- 回放结果必须保存 known_at_bar_index；确认时间晚于图形起点时，不得提前显示。
- 策略信号默认在当前 K 线收盘后产生，默认下一根 K 线开盘成交；其他模式必须在 run.json 显式记录。
- 完成的回测 run 不可覆盖。

## 5. UI 强制规则

- 单一 ChartGroup 内包含一个主图和零到多个副图，默认三窗格比例为 6:1:1。
- 所有窗格共享时间轴与垂直十字线，纵轴相互独立。
- 只允许最下面的可见窗格显示时间轴标签。
- 相邻窗格分隔线拖动时一边增大、另一边等量减小。
- 右侧面板和底部面板展开时压缩图表，不得覆盖图表。
- 主图不可删除；副图可新增、删除、折叠、最大化和重排。
- 用户绘图与 Python 生成的策略对象是不同对象模型。
- 缠论在对象树中作为一个 StrategySource 分组，不为每一笔和每个中枢创建普通绘图节点。
- 图层和命中检测遵守 docs/04-ui-layout-and-layers.md。

## 6. API 与契约

- 公共接口统一使用 /api/v1。
- Go 到 Python 的内部接口统一使用 /internal/v1，只监听 127.0.0.1。
- 所有请求携带或由服务生成 request_id 和 trace_id。
- 长任务立即返回 job_id 或 run_id，Vue 通过轮询查询；首期不得引入 WebSocket。
- 所有跨进程 JSON 字段使用 snake_case，时间使用 UTC 毫秒或 RFC 3339，具体以 Schema 为准。
- 修改契约时先改 contracts，再改两端实现和契约测试。

## 7. 日志

- 三端使用结构化 NDJSON。
- 每条日志必须有 source_file、source_line、source_function。
- 每个活动日志文件最多 50 MiB，当前文件加备份最多 10 个，即 backup_count 为 9。
- 日志失败不得中断导入、图表、指标或回测。
- INFO 不得逐根打印完整 K 线；批量流转只记录范围、数量、校验和和耗时。
- 阶段信号、交易信号与正式回测结果必须另存到 runs，不可只写滚动日志。

## 8. 工程质量

- Go：go test ./...、go vet ./...、格式化和静态检查通过。
- Python：单元测试、类型检查、格式检查通过；算法必须有前缀不变性测试。
- Vue：类型检查、单元测试、组件测试和关键 E2E 通过。
- 所有 JSON Schema 示例必须校验通过；OpenAPI 必须能被解析。
- 文件写入采用临时文件加同目录原子重命名；成功目录最后写 _SUCCESS。
- 路径输入必须规范化并限制在 data_root 内，拒绝 .. 和符号链接逃逸。

## 9. 禁止的捷径

- 不得加入数据库、Redis、消息队列或 WebSocket 来替代首期文件方案。
- 不得把三个窗格做成互不相关的独立时间轴。
- 不得根据屏幕当前显示多少根 K 线决定缠论历史计算起点。
- 不得用固定 500 根预热宣称与全历史缠论严格一致。
- 不得把缓存文件当作正式回测记录。
- 不得在日志中记录完整数据批次、访问令牌或用户敏感路径。

## 10. 完成定义

每个里程碑的提交说明必须包含：

- 实现了哪些验收项。
- 修改了哪些契约。
- 执行了哪些测试及结果。
- 已知限制。
- 是否生成或更新了可重复使用的样例数据。

